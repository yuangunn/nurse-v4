# Changelog

NurseScheduler v4의 주요 변경 이력. 최신 버전이 위쪽.

---

## v4.11.0 — 2026-08-24

**"인원은 딱 맞는데 왜 안 되지"에 숫자로 답한다. 주휴는 4주 내내 한 요일을 지킨다.**

### 🔢 쉴 코드 부족을 솔버 없이 산술로 확정
- 주휴를 안 넣으면 남는 인력이 쉴 코드가 없어 실패하는데, 진단이 13단계를 다 쌓은
  뒤 마지막 상한에서 터져 **`생리휴가(생) 제약 충돌`로 오진**됐다. V·생이 그 전까지
  사실상 무제한이라 앞 단계가 전부 통과하기 때문.
- 주별 `휴무 수요(하한)` vs `공급 상한(확정 휴무 + OF 1 + V + 생 + P1)`을 재서
  부족이 나오면 **확정 판정**한다. 필요/가능 칸 수와 해결 경로가 함께 나온다.
  ```
  [원인] 잉여 인력이 쉴 코드가 없습니다 (산술 확정).
    · 1주차 (03-01~03-07, 재적 6명): 휴무 필요 21칸 / 놓을 수 있는 휴무 16칸 — 부족 5칸
  → 해결: 분석 탭 '주휴 추천 배분 → 사전입력에 적용' 후 다시 생성하세요.
    (인원을 늘리면 오히려 악화됩니다 — 쉴 사람이 늘어납니다.)
  ```
- 13단계를 **먼저 돌린다** — 금지 전환처럼 셀을 짚은 원인은 가리지 않고 함께 붙이고,
  꼬리 단계 오진(생리휴가·홀짝월·월 최대 야간)일 때만 대체한다.
- **신호등**(`/api/feasibility`)도 솔버 전에 즉시 판정한다.
- 거짓 부족 두 경우는 판정하지 않는다 — 월 경계에 걸친 주(V·생 월 한도가 다음 달
  몫이라 공급이 더 크다) · 주휴를 auto_assign 으로 열어 둔 구성.

### 📅 주휴 — 블록(1~4주기) 안에서는 한 요일
- 병동 관행: 1~4주기 동안 주휴 요일이 같고, **4주기→1주기로 넘어갈 때만** 하루 당긴다.
  당긴 요일에 자리가 없으면 **그 블록 4주를 통째로** 다른 요일로 옮기고, 옮긴 요일이
  다음 블록의 기준이 된다.
- 분석 탭 추천이 종전에는 회전 요일에 여유가 없으면 **그냥 건너뛰었다** — 경고조차
  없어서 주휴가 빠진 줄도 몰랐다. 반대로 요일 미지정 간호사는 그 주만 다른 날로 빼서
  '4주 동일 요일'이 깨졌다. 둘 다 블록 단위로 통일했다.
- 옮길 요일은 여유(재적 − 그날 필요 인원)가 큰 쪽 — 토(4/3/2)·일이 자연히 뽑혀
  **'주말에 많이, 주중에 적게'**가 별도 설정 없이 나온다.
- 엔진도 같은 규칙을 지킨다(`juhu_block_lock`, 기본 켜짐). 단 **주휴 무시(재배치)를
  켰을 때만** 적용된다 — 그냥 생성되면 사전입력 주휴는 손대지 않는다.
- 새 토글 **주휴 이동 제한 풀기** — 정말 안 풀릴 때 요일 고정을 해제한다.

### 🧪 회귀 CI
- push/PR 마다 **수집 확인 → pytest → JS 검증 5종**을 돌린다. 수집 확인을 따로 두는
  건 직전 사고가 '실패'가 아니라 **수집 실패**여서 11건이 조용히 안 돌았기 때문.
- `requirements-dev.txt` 신설 — `pytest`·`httpx` 가 어디에도 선언돼 있지 않았다.
- standalone 빌드가 코어·양식과 어긋나면 CI 가 실패시킨다.
- 회귀 137 → **153건**.

### 📖 문서
- `MANUAL.md` 를 v4.5.0 기준 → 현재로 (243 → 468행). 근무 유형표의 P1 누락·
  V/생 '수동' 오기 정정, 저장탭 업로드·멀티월 붙여넣기·명부 xlsx·신호등·임산부·
  야간전담·오프특근 절 신설, **5장 '이 프로그램이 병동 규칙을 보는 방식'** 추가.
- `docs/milestones.md` 가 v4.9.0 에서 멈춰 출하된 결정과 모순돼 있던 것 정정.

---

## v4.10.2 — 2026-08-20

**명부에서 "이 사람이 어느 달 나이트킵인지"가 보인다.**

### 🌙 야간전담 뱃지 — `3월 야간전담` / `3,5월 야간전담`
- 설정 탭 명부의 야간전담 칸이 **보고 있는 달의 on/off(`전담`·`—`)만** 보여줘서,
  다른 달에 나이트킵이 잡혀 있어도 명부에서는 알 수 없었다.
- 이제 지정된 달을 그대로 뱃지로 보여준다: `9월 야간전담`, `9,10월 야간전담`,
  달이 많으면 `9월 외 7개월 야간전담`(당월 포함) / `7개월 야간전담`(당월 아님),
  다른 해가 섞이면 `9월 · 2027년 1월 야간전담`, 월별 지정 없이 켜진 구형 설정은
  `상시 야간전담`. 전체 목록은 툴팁에 그대로 있다.
- **당월 포함 여부는 색으로** 구분한다 — 당월이면 진한 강조, 다른 달만이면 옅게.
  클릭하면 종전대로 **보고 있는 달**을 켜고 끈다.
- '오늘' 탭의 근무자 메타도 `is_night_shift`(상시 플래그) 대신 **그 달 기준**으로
  야간전담을 표시한다.
- 검증: `node scripts/test_night_badge.mjs` 9케이스 + 실제 화면(라이트/다크) 확인.

---

## v4.10.1 — 2026-08-20

**나이트킵을 마친 사람이 다음 달 야간을 못 받던 버그 수정.**

### 🌙 나이트킵 달의 야간은 홀짝월 합산에서 제외
- 홀짝월 합산(전월+당월 ≤ 11회)은 **수면오프 회피** 규칙인데, 야간전담(나이트킵)
  달의 야간까지 합산해 버려서 **1월 나이트킵(14회) → 2월 일반 전환 시 야간 상한이
  0**이 됐다. 그 사람만 다음 달 야간을 아예 못 받고, 야간 가능자가 좁은 구성에서는
  "홀짝월 합산 야간 11회 제약 충돌"로 생성이 실패하기도 했다.
- **나이트킵 때 한 나이트는 수면오프와 무관하다**(제1원칙 7). 이제 전월이 그
  간호사의 야간전담 달이면 그 달 야간은 합산에서 뺀다.
- 두 곳에서 막는다: **전월N 자동 인수인계**(`compute_prev_month_nights` — 저장본의
  그 달 명부로 나이트킵 판정)와 **엔진**(`_two_month_rhs` — 수기 입력·구버전 저장본
  방어). 사전입력 라인트·정밀 분석 라벨도 같은 기준.
- 일반 근무로 쌓은 전월 야간은 종전대로 합산에 들어간다 — 규칙 자체는 그대로다.
- 회귀 133 → **137건** (전월 나이트킵 → 당월 야간 가능 · 일반 전월 야간은 여전히
  차감 · 전월N 인수인계 제외, 양 엔진).

---

## v4.10.0 — 2026-08-20

**오프특근을 제대로 고쳤다.** v4.8.0·v4.9.0에서 두 번 다 "공휴일이 낀 주"를 조건으로
잡았는데, 오프특근은 공휴일과 무관하다. **경가·조가 등으로 휴무가 갑자기 많아지면
남은 근무자가 오프를 줄여가며 근무를 뛰는 것**이고, 그때도 깎이지 않는 최소 보장이
주휴다 (제1원칙 3, 사용자 재설명).

### 🔁 오프특근 = 어쩔 수 없을 때의 마지막 수단
- 주 1회 OF는 **최대한 지키는 의무**가 됐다. 모델은 `Σ주간 OF + s = 1`(s = 오프특근)
  에 **압도적 페널티**(어떤 배점보다 큰 −1,000,000)를 걸어, 그러지 않으면 근무표가
  성립하지 않을 때만 s가 켜진다. 주 2회 금지(상한)는 종전대로 하드.
- 공휴일·법휴 조건은 **전부 제거**했다. 연휴 주에 법휴를 넣어야만 풀리던 v4.9.0의
  제약도 사라진다.
- 결원이 몰린 달은 이제 **실패하지 않고 생성된다** — 대신 누가 오프를 반납했는지
  숨기지 않는다.

### ⚠ 오프특근 리포트 (신규)
- 결과에 **⚠ 오프특근 N건** 패널이 뜬다: 간호사·주차·기간. 생성 메시지에도 요약이
  붙는다. 수당·보상은 사람이 처리해야 하므로 **누가 반납했는지 반드시 보이게** 했다.
- 최소 보장인 **주휴는 그대로** — 엔진은 사전입력된 주휴를 건드리지 않는다.

### 🩺 함께 정리한 것
- 사전입력 경고에서 "OF 0회"를 뺐다 — 규칙 위반이 아니라 오프특근의 결과다
  (분석 탭·사전입력 라인트·`pinned_notes` 모두).
- 정밀 분석은 주별 OF를 **상한(2회 금지)만** 짚는다. OF 1회 의무는 슬랙으로 양보되니
  더 이상 '생성 불가의 원인'이 될 수 없다.
- 진단 Phase 8 주간 산식은 의무 휴무를 주휴 1로만 센다(거짓 '공급 부족' 제거).
- 회귀 131 → **133건** (부족 시 최소한만 반납 + 리포트 / 여유 있으면 전원 1회 /
  공휴일 OF 금지 불변, 양 엔진).

---

## v4.9.0 — 2026-08-20

**오프특근을 원문대로 조이고, 앱이 사전입력을 훈계하지 않게 했다.** v4.8.0에서
단순화했던 오프특근 조건을 제1원칙 3 원문("그 주에 법휴가 부여되었다면")대로
좁히고, 월경계 주에서 새던 공휴일 인식을 고치고, 확정 사전입력에 대한 빨간
경고를 '참고'로 내렸다.

### 🔁 오프특근 — 근거 있는 사람만
- v4.8.0: 공휴일이 낀 주면 **누구나** OF를 뺄 수 있었다(단순화).
- v4.9.0: **그 주 공휴일에 `법`(법정공휴일 휴무)을 받았거나 실제로 근무한
  간호사만** OF를 뺄 수 있다. 연휴 내내 근무도 법휴도 없이 지낸 사람은 종전대로
  주 1회 OF 의무 — "쉬지도 일하지도 않은 사람이 OF를 뺀다"는 구멍을 막았다.
  모델에서는 `Σ주간 OF + Σ(그 주 공휴일의 법·근무) ≥ 1`로 걸린다(양 엔진 동일).
- 연휴 주가 안 풀릴 때 **"공휴일에 법휴를 넣어 쉴 사람을 정해 주세요"**를 세 곳에서
  안내한다: 진단(공휴일 주별 면제 대상 인원), 분석 탭, 정밀 분석 원인 라벨.

### 📅 월경계 주의 공휴일을 이제 본다
- 근무표는 주기(7일) 단위라 전월 말·익월 초가 함께 계획되는데, **공휴일만 당월로
  잘라서** 월경계 주에 걸린 신정·설날·삼일절을 못 봤다(연 3~4회 발생).
  그 결과 ① **다음 달 공휴일에 OF/V/생이 배정**되고(앱 자신의 "공휴일 OF 금지"를
  어긴 표가 인쇄물로 나감) ② 오프특근 판정에서도 빠졌다.
- 이제 공휴일은 **생성 주기 범위** 기준이다. 사전입력 탭의 [🇰🇷 공휴일 자동입력]도
  주기 전체(전월 말·익월 초 포함)를 채운다.

### 📌 사전입력 경고 — '위반'에서 '참고'로
- 사실-클램프(v4.7.0) 이후 엔진은 **확정된 사전입력을 사실로 수용**한다. 그런데
  화면 경고는 예전 기준 그대로라, 완성된 번표를 붙여넣으면 **🟢 생성 가능 신호등과
  빨간 경고 벽이 동시에** 뜨는 모순이 있었다.
- 이제 확정 셀만으로 정해지는 항목(금지 전환·자격·V/생/야간 월한도·연속 근무/야간·
  N→휴무→D·주 OF 횟수·일별 인원 초과)은 **📌 참고(회색)**로 표시된다 —
  "규칙과 다르지만 확정으로 수용됩니다". 셀에는 점선 테두리.
- 빨강(⚠ 생성 불가)으로 남는 건 **알 수 없는 근무 코드** 하나뿐이다 — 실제로 생성을
  막는 것만 빨강이다. 분석 탭 구조 경고도 같은 기준.

### 🩺 문서·자잘한 수정
- CLAUDE.md 하드 제약 표 정정: **주휴(주)는 엔진이 배정하지 않는다** — 사전입력
  전용이고, 주당 `≤1` 강제는 주휴 재배치 완화를 켰을 때만 존재. "매주 정확히 1회"로
  적혀 있던 것을 사실대로 고쳤다.
- 진단 Phase 8 주간 총량 산식이 오프특근을 반영(면제 주는 주휴 1만 의무) — 거짓
  "주간 총량 부족" 경고 제거.
- 결과 화면의 Alpine 콘솔 오류 7건(`?.` 가드 누락) 정리, README 다운로드 용량을
  실제 산출물 크기로 갱신.
- 시뮬레이터가 병동처럼 **공휴일에 법휴를 부여**한다(`beop_pins`).
- 회귀 128 → **131건** + 프론트 라인트 시나리오 13 → **15건**.

---

## v4.8.0 — 2026-08-20

**병동 도메인 제1원칙 + 오프특근 규칙.** 현장에서 확인받은 병동 운영 사실을
프로젝트 제1원칙으로 못박고, 그중 하나인 **오프특근**을 엔진 규칙으로 구현했다.
공휴일 연휴가 낀 주에서 구조적으로 풀리지 않던 교착이 해소된다. 사전입력 신호등이
엔진과 다른 기준으로 판정하던 거짓 빨강도 함께 수정.

### 🔁 오프특근 — 공휴일이 낀 주는 OF 0회를 허용
- 종전: 완전한 주는 **OF 정확히 1회**. 공휴일에는 OF 배정이 금지되므로, 3~4일
  연휴가 낀 주는 남은 비공휴일 자리에 주휴 + 전원 OF가 몰려 **비공휴일 캐파가
  정확히 만석** → 야간전담이 쉴 자리가 없어 생성 자체가 실패했다 (2026-09 추석·
  2027-02 설연휴 재현).
- 변경: **당월 공휴일이 포함된 완전한 주는 OF `≤1`** (0회 허용) — 병동에서 실제로
  쓰는 오프특근(불가피할 때 주 1회 OFF를 빼는 관행, 그 주에 법휴가 부여된 경우)을
  그대로 모델에 넣은 것. 공휴일 OF 금지·주휴 주 1회는 그대로다.
- HiGHS·CP-SAT 양 엔진 + 정밀 분석(게이트 라벨) + 성공 결과 참고 안내(`pinned_notes`)
  + 프론트 경고 2곳(분석 탭 구조 경고·사전입력 라인트)까지 동일 기준으로 동기화 —
  이제 어느 화면도 공휴일 주의 OF 0회를 위반이라고 말하지 않는다.

### 🚦 신호등 거짓 빨강 수정
- 사전입력 신호등(`POST /api/feasibility`)이 **사실-클램프 없는 옛 모델**로 판정해,
  엔진은 그대로 생성해 주는 입력(한 주에 신청 OFF 2개, 확정된 E→D 쌍 등)에
  🔴 생성 불가를 띄우던 문제. 프로브가 생성 엔진과 같은 클램프를 쓰도록 수정.
- 정밀 분석(`/api/diagnose`)·자동 수정 제안은 의도대로 클램프 없이 유지 — "규칙과
  다른 곳을 전부 짚어 달라"는 용도이기 때문.

### 📋 병동 제1원칙 (CLAUDE.md 최상단)
- 공휴일에도 병원은 돌아간다(법휴·주휴·근무 모두 가능, 전원 휴무 불가) · 오프특근 ·
  요일별 **최소** 인원은 18명 기준(일 3/4/3 월 4/5/3 화~목 5/5/3 금 5/4/3 **토 4/3/2**) ·
  주휴를 공휴일에 몰아주는 관행은 없다(주휴·공휴 중복은 수당으로 보상) ·
  대학병원 3교대 외과병동(주말 전원 OFF 불가) · 사전입력(원티드)에는 반드시 사유가
  있다(V 자동 대량 사용은 납득 불가 — 원티드 미반영 안내·주휴 조정이 우선) — 7건.
- **토요일 기본 인원 3/3/2 → 4/3/2** (새 프로필 시드). 기존 프로필은 저장된 값을
  그대로 쓰므로, 설정 → 요일별 인원에서 토요일을 한 번 확인할 것.

### 🧪 12개월 연속 생성 시뮬레이터 (`scripts/sim_chain.py`)
- 18명(임산부 1·야간전담 2 매달 교대·차지 가능 9) 구성으로 전월 생성분을 이어붙여
  N개월을 연속 생성 — 주휴 4주 순환 드리프트가 생성을 막는지 검증하는 도구.
  실행: `python3 scripts/sim_chain.py [개월수] [엔진]`.
- 결과: **주휴 드리프트(주말 → 평일 몰림)는 생성을 막지 않는다.** 대신 비용이 V로
  전가된다 — 배점 규칙을 실제 앱과 동일하게 넣으면 5개월 V 60 → 14개로 감소
  (생리휴가가 먼저 소진되기 때문). 자세한 내용은 `docs/session_notes/2026-08-20.md`.

### 🔧 기타
- `.github/workflows/tag.yml` — 원격 세션에서 태그 push가 프록시에 막힐 때
  workflow_dispatch로 태그를 만드는 우회 경로.
- 회귀 테스트 122 → **128건** (오프특근 양 엔진 × 허용/유지 4건, 신호등 클램프 2건).

---

## v4.7.0 — 2026-08-19

**기존 번표 반입 + 완성표 생성 보장.** 이미 만들어진 번표를 앱으로 들여오는 경로
(저장 탭 업로드·멀티시트 일괄·멀티월 붙여넣기·명부 xlsx 왕복)와, 사전입력 확정분을
'주어진 사실'로 수용하는 사실-클램프가 핵심. 사전입력 실시간 신호등, assign.html v3
(문서형·NAS 공유) 포함.

### ✅ 완성·부분 확정 번표는 생성을 거부하지 않는다 — 사실-클램프
- 이미 만들어진(지나간) 번표를 사전입력에 빈칸 없이 채우고 생성을 누르면 **그대로
  생성된다** — 확정된 부분은 검증 대상이 아니라 주어진 사실. 인원이 빡빡하다는
  이유(근무 14, 가용 15 등)로 거부하던 문제 해소.
- **부분 확정도 동일**: 2주차까지만 채우고 나머지를 비워도, 하루가 전부 차 있으면
  그 날은 확정으로 수용하고 빈칸에만 규칙을 적용한다. 확정 OF 2회·V 2회 같은
  기존 확정은 수용하되, 자유 칸이 위반을 더 늘리는 것만 금지(상한 클램프).
- 규칙과 다른 부분은 성공 결과에 **참고 안내**(`pinned_notes`)로만 표시 — 일별
  인원·금지 전환·주별 OF·V/야간 한도·연속 한도·연속야간 직후 근무·N→휴무→D·
  야간전담 일수. 사전입력 완화를 명시로 켜면 종전대로 표를 고쳐서라도 맞춘다.
- **생리휴가는 보장이 아니다** — "월중 1회 주어질 수 있다"일 뿐. 야간전담 여성
  '생 정확히 1회' 강제 배정을 제거하고 월 1회 상한만 유지 (필요하면 OFF 하나를
  수기로 생으로 교체).
- 진단 개선: 구조적 휴무 부족을 생리휴가 충돌로 오진하던 문구를 '쉴 수 있는 코드
  공급 부족' 설명 + 주휴 배분 안내로 교체. 분석 탭에 주별 OF 2회/0회 **구조 경고**
   추가 — "하나를 '주'로 바꾸면 규칙에 맞습니다" 안내만 하고 자동 변환은 없음
  (주휴는 사람이 결정).
- HiGHS·CP-SAT 양 엔진 동일 적용. 회귀 122건 통과.

### 📥 저장 탭 — 이미 완성된 번표 업로드
- 엑셀 파일(xlsx/xlsm/csv/tsv) 업로드 또는 붙여넣기로 완성 번표를 저장 목록에
  바로 추가 — 솔버를 돌리지 않고, 표에서 역산한 일별 인원까지 함께 저장.
- **멀티시트 연간 번표 일괄 업로드** — 시트가 여러 개면 시트 이름·내용·파일명에서
  각 시트의 년월을 감지해 매핑 표를 띄우고, 확인 한 번으로 월별 저장본을 일괄 생성.
- 서버 표 파싱(`POST /api/parse-table-file`) — 병합 셀 전파·날짜 ISO 정규화·CP949 폴백.

### 📆 사전입력 붙여넣기 — 멀티월 해석
- 날짜에 월이 있으면(5/26 등) 그대로, 일자만 있으면 감소 지점을 월 경계로 해석해
  붙여넣은 표를 **해당되는 모든 달에** 적용 — 1달 단위로 더 이상 끊지 않는다.

### 📇 간호사 명부 — 엑셀(xlsx) 왕복
- 템플릿/내보내기가 xlsx 기본 — 성별·야간전담·주휴요일·주휴로테이션·트레이닝
  **드롭다운**과 '작성 방법' 안내 시트 포함. 내보내서 엑셀에서 고치고 그대로
  가져오는 왕복 편집.
- 가져오기는 확장자 무관 매직 바이트(PK)로 xlsx 자동 감지, 기존 CSV 경로 호환 유지.

### 🚦 사전입력 실시간 생성 가능성 피드백
- **생성 가능성 신호등** — 사전입력 편집을 멈추면(1.2초 디바운스) 백그라운드로 하드 제약만 1회 풀이(`POST /api/feasibility`)해 🟢 생성 가능 / 🔴 생성 불가 / ⚪ 미확정을 사전입력 탭 상단에 표시. 불가 시 대략적 원인 라벨 칩(클릭 → 해당 셀로 이동) + '정밀 분석' 버튼 제공.
  - 서버: `conflict_analyzer.probe()` 2단계 — ①게이팅 없는 순수 하드 모델로 빠른 판정(현실 구성 0.1~0.5초) ②infeasible일 때만 게이팅 모델 1회 풀이로 원인 라벨 추출(3초 캡, SufficientAssumptions). 생성 솔버 실행 중이거나 프로브 중복 시 busy 반환.
- **예상 생성 시간 상시 표시** — 신호등 옆에 `estimate_seconds()` 기반 예상 시간을 사전입력 건수에 따라 실시간 갱신 (선입력이 많을수록 자유 변수가 줄어 단축).
- **즉시 라인트 대폭 확장** (`_checkViolations`) — 기존 금지 전환 9종 + 인원 슬랙에 더해, 세는 것만으로 확정되는 하드 제약 위반을 입력 즉시 셀·패널에 경고: 알 수 없는 코드(트레이니 `/` 코드는 무시), 자격 미달(D↔DC 등 PRE_FLEX 반영), V 월한도, 생리휴가(남성·월 1회), 연속 근무/야간 초과, N→휴무→D 패턴, 주 OF 2회·의무 OF 배치 불가, 야간전담의 주간 근무, 월 최대 야간·홀짝월 합산 초과, 일별 D/E/N 사전배정 초과(정확 일치 위반). 솔버가 조용히 무시하는 입력(공휴일 OF, 임산부 야간·생)은 위반이 아니라 '무시됨' 경고로 구분 — 서버 `_effective_pre` 의미와 1:1.
- 검증: `tests/test_feasibility_probe.py`(4건) + `scripts/test_preinput_lint.mjs`(13 시나리오, 오탐 방지 포함) + 실서버 E2E(feasible 0.5초·금지전환 0.2초·공급과잉 3.3초).

### 📄 assign.html v3 — 컴맹 친화 문서형 전면 재설계 (다중 PC · NAS 공유)
- **대상 사용자 재정의**: 50대 컴퓨터 비숙련 차지간호사도 혼자 쓸 수 있게 — 켜면 바로 '이번 주 배정표'(병실 배정표 모양), ◀▶ 주 이동, 큰 버튼 3개(인쇄/새 근무표 넣기/고치기), 설정은 전부 ⚙ 관리 뒤로.
- **저장 구조 교체**: localStorage → **NAS의 JSON 데이터 파일 하나** (File System Access API — file://에서 동작 실측). 최초 1회 파일 만들기, 이후 [이어서 시작] + 브라우저 허용 1클릭(핸들은 IndexedDB 영속). 여러 PC에서 같은 파일 공유, rev 충돌 감지, 닫기/전환 시 즉시 저장, .json 드롭으로도 열기.
- **연속 타임라인**: 월 조각 삭제 — 전 기간을 한 번에 계산(400일 캡)해 월 경계 이음새·이월 개념 자체가 소멸. 넣기 화면은 붙여넣기 → "이렇게 읽었어요(N명, 기간)" 미리보기 → [맞아요] 확인, 날짜 헤더의 연/월 자동 추정(월 넘김 포함).
- **지난 어싸인 직접 입력**: 관리 화면에 간호사별 [마지막 어싸인 A(CN)~E | 근무 D/E/N] 큰 버튼 표 — 날짜 개념 없이 '전일 취급' 시드. 근무표에 기록이 있으면 기록이 우선.
- 주간 화면에서 이름 클릭=담당 교체(스왑), 교육·행사 칸 클릭 입력. 주간 xlsx(양식 그대로)·전체 인쇄는 관리로 이동.
- VBA에도 동일 개념 추가: 설정 시트 "■ 지난 어싸인 (직접 입력)" 표(J50~) — 전월 시트가 없거나 빠진 간호사만 이 표로 시드(기록 우선), 완료 메시지에 반영 인원 표시.
- **실시간 수정 3종 (배정표에서 직접)** — ①방 칸 클릭 = 그날 하루 병상 수정(예: 환자 감소 시 차지 `1, 12, 14`) — 화면·인쇄·xlsx 즉시 반영, 파란 밑줄 표시, 인원수 변동에도 수기값 유지 ②같은 시간대 방 겹침 자동 감지(범위 `1~3` 전개 비교) → 빨간 표시, 정리는 수동 ③이름 클릭 픽커에 '근무 바꾸기'(D~V·지움) — V 전환 시 인원수·방 구성·이후 어싸인까지 즉각 재배치. 일요일 나이트 수기 입력도 픽커로 처리.
- **Opus 4렌즈 리뷰 반영 (확정 지적 전건)** — 저장: rev를 쓰기 성공 후에만 파일 rev 기준 전진(되감김·실패 시 증가 제거), 파일 읽기 실패 시 절대 덮어쓰지 않고 보류, 충돌 취소=백업 내려받기(reload로 작업 폐기하던 것 제거), 자동 저장 경로는 확인창 없이 보류, 파일 전환 전 대기분 flush, 손상 파일 열기 차단. 날짜: `2026.08.01`식 헤더·월>12 오파싱 차단, 날짜열 항상 시간순 전진(12월→1월 연도 증가), 일자만 헤더는 넣기 화면의 **연/월 확인 UI**로 지정(미리보기에 연도 표시) — 다음 달 근무표가 이번 달을 덮어쓰던 치명 버그 해소. 흐름: 이름 변경 시 중복 이름 차단·수동교체 이관 중 예외 수정, Ctrl+Z가 수동교체·차지·시드까지 복원, 파일 미연결 상태 붙여넣기 안내, 메모리 모드 미저장 경고. VBA 지난 어싸인 표: 배정표 표기(B~E)로 통일(한 칸 밀림 방지), 미인식 행 경고, 차지 무효과 안내.
- 검증: 헤드리스 E2E(월 경계 넣기→주간 렌더→수기 시드→교체→경계주 xlsx(병합 76 보존)→인쇄) + 리뷰 재현 케이스 회귀(가드·연말 넣기·이름 삭제/중복/undo) 전부 통과.

### 👥 사전입력 엑셀 붙여넣기 — 없는 간호사는 바로 추가
- 붙여넣은 표에 간호사 목록에 없는 이름이 있으면 그 사람의 근무가 조용히 빠져 "왜 몇 명이 비지?" 하고 헤매게 된다. 경고 카드에 **[＋ 간호사로 추가하고 다시 맞추기]** 버튼을 넣어, 누르면 그 이름들을 새 간호사로 등록하고 표를 다시 맞춰 준다(근무 자격 전부 가능, 순서는 맨 뒤).
- 추가 전에 이름 목록을 보여 주고 확인받는다(머리글 같은 엉뚱한 칸이 섞일 수 있어 자동 추가는 하지 않는다). `*` 접두·중복·공백은 정리해서 합친다.

### 📤 배정표 탭 주 동작을 '엑셀로 내보내기'로 + 양식 검사 도구
- HTML 인쇄본은 고칠 수 없어서, 배정표 화면의 주 버튼을 **[이번 주 배정표 엑셀로 내보내기]**로 바꿨다(지금 보고 있는 주를 병동 양식 그대로 xlsx 로). 인쇄는 보조 버튼으로 남겼고, ⚙ 관리의 주간 내려받기는 여러 주를 받는 용도로 문구를 정리했다.
- **⚙ 관리 > 배정표 양식** 신설 — 양식 xlsx 를 올리면 지금 쓰는 양식과 비교해 ①우리가 채우는 자리(요일 줄·날짜 줄·D/E/N 구역과 줄 수·어싸인 라벨·요일당 칸 수·중간번·교육행사)가 그대로인지 ②바뀐 문구가 어디인지 ③합친 칸 변화까지 짚어 준다. **검사만 하고 저장하지 않는다**(교체 방식은 미정).
- 구조 인식은 좌표를 박아 두지 않고 시트에서 직접 찾는다(요일 글자가 4개 이상인 줄 = 요일 줄, 라벨이 세로로 이어지는 열 = 어싸인 라벨 열, 그 열이 아닌 왼쪽 열의 D/E/N = 구역, 다음 왼쪽 글자까지가 그 구역 범위). 내장 양식으로 검증하면 앱이 실제 쓰는 값(D 5행/5줄, E 11행/5줄, N 16행/3줄)과 정확히 일치.
- 그 과정에서 xlsx 셀 파서 버그를 잡았다 — 빈 칸(`<c .../>`)을 구분하지 않아 뒤쪽 칸의 값을 끌어와 표가 통째로 어긋나던 문제.

### ✍️ 만든이 서명 + 날짜 버전
- ⚙ 관리 > **정보 > 이 프로그램** 패널 — 이름·버전·만든 사람(yuangunn). 도움말 패널 하단과 시작 화면에도 같은 서명 줄. (연락처·저장소 링크는 넣지 않음)
- **버전이 곧 만든 날짜** — `v260809a` 형식(v + YYMMDD + 그날 몇 번째 빌드인지 a·b·c…). 빌드할 때 자동으로 붙고, 같은 날 다시 빌드하면 알파벳만 올라간다(z 다음은 aa). 병동 PC마다 파일이 섞이므로 번호만 보고 최신본을 가릴 수 있다. exe 도 같은 문자열을 제품 버전으로 쓰고(파일 버전은 숫자 규칙상 날짜만) 속성에 회사·저작권이 들어간다.
- 서명 문구는 `ABOUT` 상수 한 곳에서 관리 — 바꾸면 세 곳에 함께 반영된다.
- 병원 도구인 만큼 **"인터넷으로 아무것도 보내지 않습니다"** 한 줄을 함께 노출(환자 자료가 밖으로 나가지 않는다는 점을 사용자가 바로 확인).

### ❓ 도움말(위키) + 처음 안내 — 실제 화면을 녹화한 그림 포함
- 화면 **오른쪽 아래 ? 버튼**(어느 화면에서나 같은 자리, <kbd>F1</kbd>도 됨)으로 기능별 사용법을 연다. 지금 보고 있는 화면에 맞는 항목이 먼저 열리고, 찾기 상자로 걸러볼 수 있다(예: '인쇄' → 관련 7개).
- **21개 항목 / 5개 분류**: 시작하기(켜기·데이터 파일·3단계) · 이번 주 배정표(보는 법·담당 교체·병실 고치기·경고 읽는 법·인쇄) · 근무표 넣기·고치기(표 전체·근무만·모르는 표기·엑셀식 편집·트레이닝) · 우리 병동 설정(병실·방 구성·필요 인원·간호사·배정 원칙·지난 어싸인) · 그 밖에(저장·백업·다크 모드·안 될 때). 항목마다 '무엇을 하는지 → 단계 → 참고'로 정리.
- **그림은 실제 화면을 구동해 만든 것** — `scripts/make-help-media.py` 가 헤드리스 브라우저로 진짜 assign.html 을 띄우고 장면마다 상태를 만들어 찍은 뒤 GIF/PNG 로 잇는다(담당 교체·병실 고치기·근무만 붙여넣기·표기 매핑·엑셀식 편집 5개는 움직이는 GIF). 화면이 바뀌면 스크립트만 다시 돌리면 되므로 설명 그림이 낡지 않는다. 6장 합계 662KB, 빌드 스크립트가 base64 로 심는다.
- **처음 안내**: 처음 켠 사람에게 4단계 카드(파일 만들기 → 근무표 넣기 → 확인·인쇄 → ? 버튼 위치)를 한 번만 띄운다. 도움말에서 [처음 안내 다시 보기]로 언제든 다시 볼 수 있다.
- 인쇄에는 도움말·안내가 끼어들지 않게 제외(A4 1장 유지 확인).

### 🔤 데이터 파일 — 읽기는 `.json`까지, 쓰기는 항상 자동 열림 형식
- **읽기**: `.json`·`.js`, 순수 JSON·껍데기 형식을 모두 읽는다. file:// 에서 script 로는 확장자와 무관하게 **내용만 맞으면 읽힌다는 걸 실측 확인**해, 자동 열림 후보에 `assign-data.json`(및 -2 ~ -5)도 넣었다. 폴더 스캔도 `.js` 를 함께 훑는다.
- **쓰기**: 확장자와 무관하게 항상 `window.__ASSIGN_DATA={...};` 형식으로 저장(브라우저·프로그램 양쪽 동일). 그래서 예전 `.json` 파일도 **한 번 열어 고치면 그 뒤로는 켜자마자 바로 열린다.**
- 옛 형식(순수 JSON) 파일만 있으면 시작 화면에 "파일은 찾았지만 옛 형식 — 한 번 열고 고치면 다음부터 자동" 안내를 띄운다(왜 안 열리는지 몰라 헤매지 않게).

### 📂 브라우저 버전도 폴더 안 데이터 파일을 알아서 — 여러 개면 고르게
- 같은 폴더에 자동 열림 파일이 **여러 개** 있으면 켤 때 목록을 띄워 고르게 했다(권한창 없이 0클릭). 파일명 옆에 간호사 수·마지막 저장 시각을 함께 보여 어느 병동인지 바로 안다. 하나뿐이면 예전처럼 그대로 열린다. 이름 규칙: `assign-data.js`, `assign-data-2.js`, `-3.js` … (한 폴더에 병동 6개까지)
- **프로그램(exe)과 브라우저가 같은 파일 하나를 공유**하도록 exe 기본 데이터 파일명도 `assign-data.js` 로 통일. 확장자가 `.js` 인 이유는 file:// 에서 브라우저가 권한 없이 읽을 수 있는 유일한 형식이기 때문이고, 내용은 JSON 그대로에 껍데기만 씌운 것이라 exe 는 네이티브로 읽고 쓴다(`.json` 을 지정해도 그대로 동작).
- 자동으로 읽은 파일과 다른 파일을 [저장 연결]에서 고르면 확인을 받는다(엉뚱한 병동에 덮어쓰기 방지). [다시 읽기]도 열었던 그 파일을 다시 읽는다.
- 남은 제약(브라우저 규칙이라 우회 불가): `.json` 파일은 file:// 에서 권한 없이 읽을 수 없다 → 폴더째 고르는 [데이터 파일 열기](권한 1회)로 스캔하거나, `.js` 자동 열림을 쓰면 된다. 수정·저장은 여전히 세션당 [저장 연결] 1회.

### 🖥 단일 실행 파일 (Windows 네이티브 앱) — `standalone/app`
- 브라우저로 `file://` 을 열면 저장할 때마다 파일 선택·권한 확인이 필요한 게 브라우저 규칙이라 HTML 안에서는 없앨 수 없다. **자기 창 안에 렌더러(WebView2)만 얹는 네이티브 앱**으로 그 제약을 벗어났다 — 주소창도, 로컬 서버(127.0.0.1)도, 임시 파일도 없다.
- 화면(assign.html 3MB)을 exe 안에 넣고 `https://assign.local/` 가짜 주소로 **메모리에서 바로** 공급한다(디스크에 풀지 않음). 출처가 고정이라 테마 같은 화면 설정이 계속 남는다.
- 파일 읽기·쓰기는 화면이 `postMessage` 로 요청하고 exe 가 네이티브로 처리 — **권한 확인이 아예 개입하지 않는다.** 저장 규칙은 브라우저 방식과 동일(임시 파일→교체, 다른 PC 우선 저장 시 충돌 안내, 읽기 실패 시 덮어쓰기 금지). 데이터 파일은 exe 옆 `assign-data.json` 또는 `assign.ini` 의 NAS 경로, 창 안에서 네이티브 파일 창으로 교체 가능.
- 창 크기 기억, 고해상도 대응, 새 창·외부 링크 차단, WebView2 런타임 없을 때 안내. `build.cmd`(Windows) / `build.sh`(macOS·Linux 크로스 빌드) — 자체포함 단일 exe 68MB 또는 런타임 의존 4MB.
- 같은 데이터 파일을 브라우저 방식과 **공유**하므로 두 경로를 병행해도 된다(서명 없는 exe 가 막히는 PC 는 브라우저로).
- 검증: 크로스 빌드로 경고·오류 0 컴파일 + 단일 exe 생성, exe 안 리소스(화면·아이콘) 확인, 그리고 C# 핸들러와 동일한 규약의 모의 호스트로 브라우저 왕복 검증(읽기·자동 저장 rev 9→10·충돌 시 파일 보존·파일 교체·미저장 변경 보호).

### 📋 assign.html 붙여넣기 — 근무 칸만 넣기 + 모르는 표기 직접 매핑
- **근무만 붙여넣기**: 이미 표가 있으면 이름 열·날짜 줄 없이 **근무 칸만 긁어 붙여넣어도** 됩니다. 미리보기에서 [맨 윗줄 = 어느 간호사] [첫 칸 날짜]만 고르면 그 자리에 채워집니다(한 달치면 기록 다음 달 1일을, 아니면 보고 있는 주를 기본값으로 제안). 이름만 있고 날짜가 없는 표, 날짜만 있고 이름이 없는 표도 있는 쪽으로 맞추고 없는 쪽만 물어봅니다.
- **모르는 근무 표기 매핑**: 경가·조가처럼 기본 목록에 없는 표기가 들어오면 칸이 조용히 비던 것을 고쳐, 미리보기에서 표기별로 [휴무로] [휴가로] [다른 근무로 바꾸기]를 바로 고르게 했습니다. 한 번 정하면 파일에 저장돼 **다음 붙여넣기부터 자동 인식**되고, 고치기 화면 범례·색·인원 계산에도 반영됩니다. ⚙ 관리 > 근무 표기에서 나중에 바꾸거나 지울 수 있고 Ctrl+Z로도 되돌아갑니다.
- **이름 열 오인 수정**: 모르는 표기(경가/조가)로만 된 첫 열을 이름 열로 착각해 근무가 통째로 사라지던 문제를 고쳤습니다 — 이름은 다른 칸에 다시 나오지 않는다는 점으로 판정합니다. 엑셀에서 딸려온 빈 열이 기존 근무를 지우던 것도 함께 수정.
- 검증: 6가지 시나리오 E2E(전체 표·근무만·시작 간호사 이동·모르는 표기 매핑·다음 붙여넣기 자동 인식·관리 패널) + 기존 회귀 전건.

### 🛏 어싸인 연속성을 **병실 기준**으로 (라벨 기준 폐기) — 코어·앱·standalone 공통
- 지금까지는 어싸인 A/B/C 라벨을 이어받는 방식이라, 인원이 바뀌어 방 구성이 통째로 달라지는 날(5인→4인)에 **보던 병실이 끊겼다**. 예: 전날 6~9호를 본 사람이 오늘 6~10호를 이어받아야 하는데, 라벨을 물고 있는 다른 사람 때문에 튕겨나감.
- 연속성 판정을 **실제 병실 겹침**으로 교체. "A인 사람이 계속 A인 이유는 보는 환자가 같아서"라는 원칙을 그대로 구현 — 코어에 `opts.roomsFor(P, 인원수, 라벨, 날짜)`를 넘기면 전에 본 병실과 오늘 각 라벨의 병실을 대조해 배정한다. 그날 수기로 고친 방(roomOv)도 그대로 반영.
- 배치는 **겹침 총합이 최대가 되도록 정확히** 계산(할당 문제 — 라벨 ≤5라 비트마스크 DP). 원칙 우선순위(1 전일 같은 근무 > 2 근무 바뀜 > 3 오프 복귀)는 유지하되 세 계층을 한 번에 풀어, 전일 근무자가 두 자리에 무차별할 때 **오프 복귀자가 원래 보던 방을 되찾는다**.
- 원칙4(튕기기)도 라벨이 아니라 '보던 병실'을 피하도록 교체. 방 정보가 없는 자리(2인 구성·지난 어싸인 수기 시드)는 기존 라벨 기준으로 폴백해 회귀 없음.
- 검증: 5인→4인→5인 전환 시나리오(전원 방 복귀), 인원 불변 시 배정 동일, 2인 폴백, 튕기기, 라벨 기준 대조군 + standalone E2E. 성능 18명×400일 5ms.

### 🚪 assign.html 자동 열림 — 켤 때마다 폴더 고르지 않게
- file:// 에서 `fetch`는 막히지만 **같은 폴더의 `.js` 파일은 `<script>` 로 읽힌다**는 점을 이용(실측 확인) — 데이터 파일 이름을 `assign-data.js` 로 두고 assign.html 과 같은 폴더에 놓으면 **권한창·피커 없이 0클릭으로** 켜자마자 이번 주 배정표가 뜬다. 열 때마다 최신본을 읽으므로 다른 PC가 저장한 내용도 그대로 보인다.
- 쓰기만 파일 핸들이 필요하므로, 수정할 때 톱바의 [저장 연결]을 **한 번만** 눌러 파일을 고르면 이후 자동 저장. 읽기 전용 상태에서 편집을 시작하면 안내가 뜬다(조용한 유실 없음).
- 이전에 쓰던 파일의 권한이 이미 남아 있으면 클릭 없이 그대로 재연결(`queryPermission`). 관리 > 데이터 파일에 [다시 읽기](다른 PC 저장분 받기) 추가.
- 데이터 파일은 `.json`(기존)과 `.js`(자동 열림) 둘 다 읽고 쓴다 — 확장자에 맞춰 저장 형식 자동 선택. [처음 시작]의 기본 이름을 `assign-data.js` 로 변경.

### 🔁 배정표 담당 교체 픽커 — 누구와 바꾸는지 보이게
- A(CN)·B·C·D·E 순서로 정렬하고 각 자리의 **간호사 이름과 병실**을 함께 표시. 본인 자리는 '(본인)'으로 비활성. 기존엔 라벨 버튼만 있어 누구와 맞바꾸는지 알 수 없었다.

### 🌙 assign.html 다크 모드
- 화면 색 전체를 CSS 변수로 토큰화하고 본 앱(frontend/css/tokens.css)의 다크 팔레트를 이식 — 표면·글자·격자·신호색·근무 21종 코드 색까지 다크 변형 적용. 톱바의 ◐ 버튼으로 **자동(기기 설정 따름) → 다크 → 라이트** 순환, 기기별 localStorage 저장(데이터 파일과 무관).
- **인쇄는 항상 라이트**: A4 시트와 내장 양식 xlsx는 테마와 무관하게 기존 색 그대로 — 다크 화면에서 인쇄해도 게시용 출력물은 동일.
- 검증: 다크/라이트 스크린샷(주간·고치기·관리), 다크 상태 인쇄 PDF 라이트 확인, 기능 회귀 E2E 전건 통과.

### 🛏 assign.html 병실 목록 — 세로 나열로 정리
- 방을 가로로 나열하던 표가 방이 많아지면 패널을 뚫고 나가던 문제 수정 — 방 1개=1행(호실·병상수·삭제)의 세로 표로 교체. 방이 몇 개든 아래로만 늘어난다.

### ✨ 스케줄 탭 — 핀포인트 수정 반영 재조정 + 사전입력 '이 표대로 인원' 고정
- **수정 반영 재조정**: 완성된 근무표에서 몇 칸을 직접 고친 뒤 [✨ 수정 반영 재조정]을 누르면, 고친 칸은 잠그고 표 전체를 사전입력(유지 보너스)으로 되먹여 재생성 — 인원·규칙을 다시 맞추는 데 필요한 최소한의 칸만 따라 움직입니다. 일별 인원 기준은 수정 전 표의 실제 인원(설정 기본값 무시). 결과에 고정 칸(노란 테두리)·따라 고친 칸(파란 점선)을 표시하고 변경 상세를 메시지로 보여줍니다. 수정으로 그날 인원이 어긋난 사전입력은 완화로만 풀리므로 재조정은 항상 완화 모드로 호출 — 잠근 칸은 완화에서도 절대 고정.
- **이 표대로 인원 고정** (사전입력 툴바 📌): 이미 완성된 근무표를 붙여넣었을 때, 일별 D/E/N 필요 인원을 설정된 요일별 기본값 대신 그 표의 실제 인원수로 일괄 고정(per-day override). 신호등·라인트·생성이 모두 이 표를 기준으로 동작. Ctrl+Z 취소 가능.
- 알려진 한계: 차지 전용 슬롯(그 시간대 요구 1명)에 일반 코드(E 등)를 핀하면 모순이라 진단 메시지가 안내 — 차지를 의도하면 EC/DC/NC로 핀. 잠금=정확 고정 계약은 기존 그대로.
- 검증: `tests/test_readjust.py`(핀 유지·인원 보존·최소 파급 ≤8/42칸, D→OF 시나리오) + CP-SAT 패리티(핀 유지·파급 7칸) + 전체 스위트 91건 통과. 서버 무변경 — 기존 유지 보너스·잠금·완화 조합만 사용.

### 🎨 assign.html 디자인 리뉴얼 2차 — 나머지 4개 화면 + 폴더 자동 감지
- **시작 화면**: 3버튼 위계(이어서 시작=딥 잉크 + 파일명·마지막 연 시각 부제 / 파일 열기 / 처음 시작 — 이어서가 없으면 파일 열기가 주 동작으로 승격), 번호 안내 3단계 카드, 드롭 안내 띠, 오류는 빨간 띠로.
- **폴더 자동 감지 (신규)**: '데이터 파일 열기'가 폴더 선택으로 동작 — 폴더 안 어싸인 데이터(.json, app:'assign')를 자동 스캔해 1개면 바로 연결, 여러 개면 파일 선택 카드(이름+수정 시각). 폴더 핸들을 기억해 다음부터 [이어서 시작] 1클릭이면 지난 파일 자동 연결, 파일이 바뀌었으면 다시 선택지 제시. 폴더 선택 미지원 브라우저는 기존 파일 픽커로 폴백. (브라우저 보안상 실행 시 클릭 1번의 권한 확인은 생략 불가)
- **새 근무표 넣기**: 단계 카드 2개(Ctrl+C/Ctrl+V 키 칩) + 드롭 영역, 트레이닝 /D /E /N 안내 띠, 읽은 결과 카드(통계 3칸: 간호사·기간·새 이름), **연·월 확인 카드** — 날짜 줄에 연도가 없으면 노란 경고 톤, 있으면 회색 '읽은 시작' 톤. 미리보기 표 새 격자 + 요일색.
- **근무표 고치기**: 월 세그먼트 + 상태 칩(N명·N칸 / 인원 안 맞는 날 N일·정원 맞음) + 되돌리기 버튼, 범례를 3묶음(근무/휴무/트레이닝)으로, 날짜 헤더 2단(일자+요일)·주말 열 틴트, 푸터는 맞는 날 숫자만·안 맞는 날만 빨간 실제/필요. 셀 색·키보드·드래그·undo 무변경.
- **관리 화면 전면 재구성**: 아코디언 9개 → 왼쪽 5묶음 목록(상태 배지: N명·N실·N켬·N건) + 오른쪽 단일 패널. 간호사 관리는 가능근무 켜짐/꺼짐 칩 + 주지 않을 방 빨간 칩(× 즉시 삭제, ＋ 픽커), 배정 원칙은 설명 달린 스위치 행(3·4 상호배제 유지), 파괴적 버튼은 빨간 글자. 선택 패널은 로컬 변수(adminPanel) — 파일 스키마 불변.
- 검증: 전 화면 E2E(패널 9종 오류 0, 배지·스위치 상호배제·금지 방 칩 삭제, 가짜 폴더 핸들로 스캔 2건·선택 카드) + 주간·인쇄(A4 1장) 회귀 + 코어 테스트 전건 통과.

### 🎨 assign.html 디자인 리뉴얼 — 핸드오프(1a 주간 화면 + A4 인쇄 시트) 구현
- **디자인 언어 교체**: YGinvest 토큰(딥 잉크 `#0F1115` · 쿨그레이 페이퍼 · Pretendard) — `standalone/design_handoff_assign_redesign/` 명세의 1a(보수적)안. Pretendard Variable을 base64로 내장(빌드 스크립트가 `frontend/fonts/`에서 주입, 파일 약 3MB)해 인트라넷 오프라인에서도 동일 서체.
- **주간 화면(1a)**: 격자선을 옅게(`#EBEDF0`), D/E/N 레일 색, 요일/날짜 2단 헤더 + 오늘 열 파란 틴트·`오늘` 칩, 교육·행사 행 앰버, 주 이동 세그먼트(`2026. 8. 2 – 8`), 버튼 이모지 제거·동사구 통일, 주 동작(이번 주 인쇄)만 딥 잉크. 하단 힌트에 수동 교체(빨강)/방 수정(파랑) 범례.
- **경고 카드**: 텍스트 나열 → 개수 배지 + 항목 목록 카드(`#FEEFEF`), 문구를 사람 말투로("8/5 오유진 — E는 가능 근무가 아닙니다. 관리 > 간호사 관리에서 확인하세요.").
- **톱바**: 브랜드 마크 + 저장 상태 칩(저장됨=초록/경고=빨강) + 배정표·관리 탭 활성 표시.
- **A4 인쇄 시트 재설계**: 사방 여백 정확 10mm, 내용 190×277mm를 flex로 정확 배분(제목 띠 9 + 주간표 129.1 + 물품체크 44.5 + Team Play 6.5 + CPR 35.4(글자 크기 역산) + 화재 auto + 신축 간격 + 대피 띠). 글자 6.8~13pt로 상향(기존 40% 축소 4.5~8.4pt), 새 색 매핑(제목 딥 잉크 띠, 구분 D/E/N 근무색). 문구·CPR 도해는 계속 내장 양식(xlsx)에서 읽음 — 양식 수정 시 build 스크립트만 재실행.
- 동작 무변경(클릭 타깃·키보드·undo·저장 파이프라인·store 스키마·xlsx 다운로드 그대로). 검증: 기능 회귀 E2E 전건 + 인쇄 1장/전체 6장 페이지 수 + 화면·인쇄 스크린샷 대조.

### 🖨 assign.html 인쇄 — 양식 원본 서식 그대로 재현
- 인쇄가 회색 표로만 나와 엑셀 출력물과 딴판이던 문제 수정 — 양식 파일의 실제 서식을 추출해 1:1 재현: 제목 보라 바탕 흰 글씨, 구분줄 파랑, 물품체크 B/C/D/E 색 글자, CPR 초록, 화재 빨강 바탕 세로 글씨, 응급상황 연초록 띠, 일=빨강·토=파랑 요일, 병합 구조(17열)·행 높이·글꼴 크기(원본 pt×40%, 엑셀 인쇄 배율과 동일)까지 원본 값 사용.
- **CPR 그림을 원본 위치·크기로**: 양식의 J26:Q30 영역(오른쪽 절반)에 크게 — 기존엔 96px로 구석에 있어 안 보였음.
- `print-color-adjust: exact` 적용 — 브라우저 인쇄 설정에서 '배경 그래픽'을 켜지 않아도 색이 항상 인쇄됨.
- 고정 문구·라벨은 전부 내장 양식 파일에서 읽으므로 양식 수정 후 build 스크립트만 재실행하면 인쇄도 따라감.

### 🖨 assign.html — 브라우저 메뉴/Ctrl+P 인쇄 백지 수정
- 인쇄 영역이 🖨 버튼을 눌러야만 채워져서 브라우저 메뉴·Ctrl+P로 인쇄하면 **백지**가 나오던 버그 수정 — 주간 화면을 그릴 때마다 현재 주를 인쇄 영역에 미리 채워, 어떤 경로로 인쇄해도 병실 배정표 양식 그대로 나옴 (헤드리스 인쇄 PDF로 백지→양식 재현·검증).

### 🚪 assign.html — 방 클릭 토글 픽커·병실 커스터마이징·금지 방·트레이니 표기
- **방 토글 픽커**: 배정표에서 방 칸을 누르면 병실 목록(1001~1014)이 떠서 클릭으로 넣고 빼기 — 선택한 방 수와 **병상 합계**(몇 명까지 보는지)를 실시간 표시. 직접 입력(자유 텍스트)·자동 복귀도 픽커 안에서.
- **병실 목록 커스터마이징** (⚙ 관리): 호실·병상수 표를 직접 수정/추가/삭제 — 101병동이 아닌 다른 병동 호실로 통째로 교체 가능(4자리 동일 접두면 축약 표기 자동 유지). 기본값 = 1001~1014 (1009=1인실 등 실제 병상수).
- **간호사별 주지 않을 방** (⚙ 관리 > 간호사 관리): 격리방·중증방·신규 제외·환자 거절 용도 — 그 방이 포함된 어싸인을 자동 회피(코어 `opts.avoid`). 연속성 원칙보다 우선하되 소프트(대안이 정말 없으면 배정하고 주간 경고로 알림). 수동 교체·DC/EC/NC 표시자는 회피보다 우선. 회피 배정이 불가피하면 서로 문제없는 상대와 자동 맞교환.
- **트레이닝 근무 `/D` `/E` `/N`**: 근무표에 입력하면 회색으로 표시만 되고 **인력 수·어싸인에서 제외** — 넣기/고치기 화면에 안내문 추가. xlsm VBA는 원래부터 `/` 접두 제외라 수정 불필요.
- 검증: 코어 avoid 6케이스 + 헤드리스 E2E(픽커 토글·병상 합계·금지 방 회피/경고·트레이니 제외·타 병동 커스텀) + 인쇄 PDF 재현.

### 👥 assign.html 간호사 관리 — 가능근무·시간대별 차지 (xlsm 기능 파리티)
- **간호사별 가능근무 6종 토글** (⚙ 관리): DC/D/EC/E/NC/N 각각 O/· 클릭 — xlsm 설정 시트의 O 표기와 동일 체계. 구버전 데이터 파일(차지 가능 여부만 저장)은 열 때 자동 변환.
- **시간대별 차지 자격**: DC만 O인 선임은 N 근무에서 차지로 뽑히지 않음 — 코어 `chargeCapable`이 `{D,E,N}` 시간대별 판정 지원(불리언 하위호환), 앱 어싸인 탭·VBA(`|PP&C|` 검사)도 동일 의미로 통일.
- **요일별 필요인원 표** (⚙ 관리, 월~일 × D/E/N) + 고치기 화면 하단 인원 카운트를 `실제/필요`로 표기, 미달·초과 시 빨강.
- **주간 화면 경고 박스**: 가능근무에 없는 근무 배정·인쇄 양식 N칸(3개) 초과·어싸인 없는 헬퍼를 그 주 범위에서 자동 안내.
- 검증: 코어 시간대별 차지 3케이스 + 헤드리스 E2E(차지 제외·경고 표시·필요인원 빨강) 통과.

### 🔁 앱 어싸인 탭 — 전월 연속성 자동 이월
- 어싸인 계산 결과의 '간호사별 마지막으로 본 방'을 프로필·월별로 localStorage에 저장하고, 다음 달 계산 시 자동 시드 — 원칙 1~4가 월 경계를 넘어 작동 (standalone·VBA와 동일 의미). 툴바에 "↩ 전월 어싸인 N명 연속성 자동 이월" 안내 표시.
- assign-core `opts.seed` 가드 완화: 주기 이월일과 겹치는 상대 idx(0 이상)도 허용 — 겹치는 날에 현재 데이터 배정이 있으면 자연히 덮어씀. 당월 날짜 상태는 시드에서 제외, 빈 결과는 기존 상태를 덮어쓰지 않음, 1월→전년 12월 조회.
- 검증: 코어 겹침/범위밖 시드 케이스 + 앱 글루 단위 테스트(`scripts/test_assign_app_seed.mjs`, 상태 저장·시드 산출·경계 6케이스) + standalone 회귀.

### 🌐 assign.html v2 — 단일 파일 통합본 (인트라넷 주력, 외부 의존 0)
- **엑셀식 편집 그리드** — 엑셀 범위 복사 → 창에 바로 Ctrl+V(이름 열·날짜 행 자동 인식, 별칭·`D/A` 선입력 파싱), 셀 키 입력(D/E/N/O/V/W, Shift=차지, 한글 ㅈㅂㅅㅌㄱ), 드래그 범위 선택→일괄 입력, Ctrl+C는 TSV+색 포함 HTML 동시 기록(엑셀에 서식째 붙음), 더블클릭 직접 입력, Ctrl+Z 40단계, 파일 DnD(.csv/.tsv, CP949 자동 폴백), 하단 일별 D/E/N 카운트.
- **년/월 선택 + 전월 자동 이월** — 월별 근무표를 localStorage에 저장, 어싸인 계산 시 전월 결과에서 각자 '마지막으로 본 방'을 시드(assign-core `opts.seed` 신설)해 원칙 1~4가 월 경계를 넘어 작동. VBA 자동 이월과 동일 의미.
- **주간 배정표 출력 2종** — ①내장 `병실 배정표.xlsx` 양식을 순수 JS(zip 파싱/패치, 라이브러리 0)로 채워 주 단위 xlsx 다운로드: 병합 76·인쇄설정(A4 40%)·이미지·고정 문구 무손실, 시트명 `YYYYMMDD~YYYYMMDD`, 날짜는 date serial로 원본 서식 유지, 예시값 선지우기, 방번호 수기 관례 축약(1001→1, 연속 `2~4`), 라벨 매핑 차지→A(CN), 월 경계 주는 이웃 달 저장분 참조. ②전 주(5~6장) HTML 인쇄 — 물품체크·CPR(이미지 포함)·화재 고정 문구를 내장 양식에서 추출(단일 소스).
- 버그 수정: `D/A` 선입력을 근무로 인식 못 해 그날 인원에서 빠지던 문제, 클립보드 복사 무음 실패(.catch+execCommand 폴백), saveCfg 예외 무방비, 원칙 체크 상태 미저장.
- 빌드: `build-assign-standalone.mjs`가 코어 + 양식 base64를 함께 주입 — 양식 파일을 고치고 재실행하면 인쇄·xlsx에 그대로 반영.
- 검증: 실브라우저 E2E(붙여넣기 파싱→배정→전월 이월 5명→경계주 xlsx→인쇄 6장), 생성 xlsx를 openpyxl(파트 15/15·병합 76·이미지 보존) + 실제 Excel 오픈으로 확인, assign-core seed 테스트 4케이스 추가.
- 요구 브라우저: Chrome/Edge 103+ (2022-06). 인터넷 불필요, file:// 더블클릭 실행.
- **결과 셀 클릭 = 수동 교환** — 배정 결과 셀 클릭 → 라벨 픽커 → 기존 보유자와 자동 스왑(앱 어싸인 탭과 동일 의미), 이후 날짜는 원칙따라 이어짐. 수동 조정은 ·표시 + 월별 저장, 셀 선입력(D/A)과 충돌 시 수동이 우선, [수동 조정 초기화] 버튼. 주간 인쇄·xlsx에도 반영.
- **백업/복원** — 모든 데이터(월별 근무표·수동 조정·설정·행사)를 JSON 파일로 내보내기/가져오기(버튼 또는 .json 드롭). 브라우저 초기화·PC 교체 대비.
- **xlsx 파일 직접 드롭** — .xlsx/.xlsm을 끌어다 놓으면 라이브러리 없이 시트 XML을 직접 파싱해 근무표로 읽음(다중 시트 선택, sharedStrings·날짜 서식(numFmt) 판정 → 진짜 date 헤더도 인식, 별칭·D/A 파싱 동일).
- **교육 및 행사 입력** — 결과 카드의 주×요일 편집기(ISO 날짜 키 — 월 경계 자동 공유). 인쇄·xlsx 양식 4행에 반영, 비우면 공란.

### 📄 Excel VBA 단독 실행 버전 전면 강화 (`standalone/assign_vba.bas`)
- **초기세팅생성** 매크로 — "설정" 시트 생성: 간호사 목록(O 표시 = 가능 근무, DC/EC/NC O = 차지 가능), 요일별 필요인원, 방 목록(유형), 인원수별 어싸인 방 구성, 원칙 2~4 on/off, 사용법.
- **근무표생성** 매크로 — 년·월 입력 → 빈 근무표 시트 생성: 간호사 행 자동, 날짜·요일 헤더, 하단 일별 D/E/N 인원 카운트(자동 수식) + 필요인원 행(일별 수정 가능, 불일치 시 빨간 표시), [어싸인 배정] 버튼 자동 부착.
- **어싸인 선입력** — 근무표 셀에 `D/A`, `E/B` 식 입력 시 해당 어싸인 고정 + 이후 날짜의 연속성 시작점 (하루만 적어도 나머지 자동). 전월 데이터 없이 독립 운용 가능.
- **어싸인배정실행** 매크로 — 근무표를 읽어 "어싸인"(그리드) / "어싸인상세"(일별 표) 시트 생성.
- 어싸인 그리드 표기를 **근무+어싸인 문자**로 개선 — 차지=`DC`/`EC`/`NC`, 방=`D/A`·`E/B`·`N/C`, 헬퍼=`D/—` (색상은 근무 구분용 보조). 색만으로는 가독성이 낮던 문제 해결.
- 설정 시트 레이아웃 정리 — "사용법"을 S열로 이동해 "요일별 필요인원" 표와 겹치지 않도록 수정.
- 설정 섹션은 앵커 텍스트(■)로 탐색 — 행 추가·이동에 안전. 설정상 불가 근무 입력·선입력 중복 등은 배정 후 경고로 표시.
- 시드 연속성 검증 케이스 추가 (`scripts/test_assign_core.mjs`).
- 실제 Excel(16.110)에서 3개 매크로 전 과정 + 원칙 1~4·선입력 시드 동작 검증 완료.
- **시트 디자인 정리 (깔끔·사무용)** — 공통 서식 헬퍼(SheetBase/TitleStyle/SectionHead/HeadStyle/BoxBorders/BottomRule) 도입: 격자선 off + 맑은 고딕, 옅은 헤더바(RGB 237,240,245), 얇은 회색 테두리, 제목 위계(네이비), 틀 고정, 어싸인상세 날짜별 밴딩. 4개 시트(설정·근무표·어싸인·어싸인상세) 전체 적용.
- **컴파일 오류 수정** — 서식 헬퍼 추가 때 모듈 레벨 `Const UI_FONT` 선언이 프로시저 뒤에 놓여 모듈 전체가 컴파일 실패(모든 매크로 실행 불가)하던 문제. 선언을 모듈 상단으로 이동.
- 설정 시트 예시 간호사 3명 → **18명** (상위 6명 차지 가능 예시, 앱 기본 시드와 동일 규모).
- **Alt+F8 목록에 매크로 3개 복원** — Excel은 파라미터(Optional 포함) 있는 매크로를 Alt+F8 목록에서 숨기므로, 사용자용 매크로(초기세팅생성·근무표생성·어싸인배정실행)를 파라미터 없는 진입점으로 분리하고 구현은 Private *_코어로 이동. 자동화 진입점(자동_*)은 코어를 직접 호출.
- **설정 시트 실행 버튼** — [근무표 생성 →] + [어싸인 배정 (최근) →] (최신 근무표_* 시트를 찾아 실행). Alt+F8은 최초 1회(초기세팅생성)만 필요.
- **원칙 번호 개편 + 원칙4(튕기기) 신설** — 차지는 번호 없이 항상 적용으로 분리, 기존 원칙2·3·4(같은 근무 유지/근무 변경 유지/오프 복귀 유지)를 원칙1·2·3으로 renumber. 신규 원칙4 = 오프 복귀자 튕기기(복귀 시 이전 방 제외, 대안 없으면 허용). 원칙3·4는 반대 개념이라 동시 O 금지 — 시트 안내문구 + 동시 O 시 원칙3만 적용하고 배정 경고 표시. 기본값 원칙4=X.
- **앱 어싸인 탭·assign.html 원칙 체계 동기화** — assign-core에 `bounceAfterOff` 규칙 추가(동시 켜짐 시 keepAfterOff 우선), 앱 탭·단일 HTML 모두 원칙 1~4 라벨 + 상호배제 토글(하나 켜면 다른 쪽 자동 해제) + 안내문구. 저장된 설정은 의미 키 기반이라 마이그레이션 불필요. 검증 3케이스 추가 (`scripts/test_assign_core.mjs`).
- **주간 배정표 인쇄 (매크로 4: 주간배정표생성)** — 병동 인쇄 양식(`병실 배정표.xlsx`, 일~토 주 단위·요일별 [방|이름] 2열)을 "양식" 시트로 자동 가져와 복제 후 채움: 3행 날짜, D(5칸)/E(5칸)/N(3칸) 섹션의 방번호·이름, 중간번(근무표 `중` 코드). 그 달을 덮는 모든 주(5~6장) 한 번에 생성, 시트명 `YYYYMMDD~YYYYMMDD`. 라벨 매핑 차지→A(CN)·A→B…, 방번호는 수기 관례대로 축약(1001→1, 연속 3개 이상 `2~4`, 구분자 `, `). 월 경계 주는 이웃 달 어싸인 시트가 있으면 참조, 없으면 빈칸. N 4명 이상이면 칸 부족 경고. 물품체크·CPR·화재 문구는 "양식" 시트에서 직접 수정하면 다음 출력부터 반영. 설정·어싸인 시트에 실행 버튼 추가.
- **전월 어싸인 연속성 자동 이월** — 어싸인 실행 시 전월 `어싸인_YYYY-MM` 시트가 있으면 간호사별 '마지막으로 본 방'을 자동 시드(전월 말일=전일로 취급). 전월 말 근무자는 원칙1·2, 월말 오프 복귀자는 원칙3·4가 월 경계를 넘어 작동. 이름 매칭, 선입력 `D/A`가 여전히 최우선, 완료 메시지에 이월 인원 표시.

---

## v4.6.0 — 2026-07-04

**어싸인(병실 자동 배정) 기능.** 근무표를 기반으로 간호사별 담당 병실을 자동 배정. 앱 탭 + 인트라넷용 단일 HTML + Excel VBA 3종 구현 (로직 단일 소스).

### 🏥 어싸인 탭 (신규, 스텝 05)
- 근무표(생성 스케줄 또는 사전입력) 기반 일별 D/E/N 어싸인 자동 배정 — 병실 단위.
- 원칙: ①차지=DC/EC/NC 표시자 우선(없으면 차지가능 최선임) ②전일 같은 근무 방 유지 ③근무 변경 시 방 유지 ④오프 복귀자 방 유지 (②>③>④, 각 원칙 on/off 가능).
- 인원수별(2~5인) 방 구성 프론트에서 편집 가능, 6인 이상은 5인 스킴 + 잔여=헬퍼. 중간번·D1·트레이니는 제외.
- 셀 클릭 = 수동 조정(같은 시간대 담당자와 스왑, 자동 복귀 가능), 일별 상세 패널, Excel용 TSV 복사.
- 설정·오버라이드는 localStorage에 프로필·월별 저장.
- 코어 로직 `frontend/js/modules/assign-core.js` (순수 함수) + UI `assign.js`. 검증: `node scripts/test_assign_core.mjs`.

### 📄 인트라넷용 별도 구현물 (`standalone/`)
- `assign.html` — 단일 파일, 더블클릭 실행. Excel 근무표 붙여넣기 → 자동 배정 → 인쇄/Excel 복사. 코어 동기화: `node scripts/build-assign-standalone.mjs`.
- `assign_vba.bas` — Excel VBA 포팅. "근무표" 시트 → "어싸인"/"어싸인상세" 시트 생성.

---

## v4.5.0 — 2026-06-17

**macOS 데스크톱 앱 지원.** Windows 전용이던 앱을 macOS(Apple Silicon)에서도 빌드·실행되도록 확장.

### 🍎 macOS 지원
- Electron `main.js`의 Windows 전용 경로(`NurseScheduler.exe` 등)를 플랫폼 분기(mac은 확장자 없는 실행파일).
- PyInstaller 스펙 아이콘을 플랫폼별(.ico/.icns)로, macOS 아이콘(`build/icon.icns`) 추가.
- `build-mac.sh` 추가 — PyInstaller 서버 번들 + electron-packager(darwin) + ad-hoc 서명 + `.app`의 zip/dmg 생성.
- 솔버(HiGHS·CP-SAT)·FastAPI·SQLite를 macOS arm64로 프리즈해 동작 검증(서버 health·프론트·API).
- CI(GitHub Actions)에 macOS 빌드 잡 추가 — 태그 push 시 Windows·macOS 산출물을 함께 릴리스에 업로드.
- ⚠️ 미서명(ad-hoc) 배포 — 유료 Apple Developer 계정이 없어 공증 없음. 첫 실행 시 우클릭→열기 또는
  `xattr -dr com.apple.quarantine <앱>` 필요. 현재 Apple Silicon(arm64) 전용.

---

## v4.4.0 — 2026-06-15

**전수 코드 분석 기반 대규모 안정화 + 신규 기능 4종.** 멀티에이전트·라인 단위 검증으로 확정된
버그 57건을 일괄 수정하고, 위시오프 파이프라인·공정성 원장·생성 관측성·공휴일 음력 자동계산을
새로 추가했습니다. 회귀 테스트 54 → 86건 통과.

### 🇰🇷 한국 공휴일 음력 자동계산
- **음력 공휴일 자동 변환** — 설날(음 1/1)·부처님오신날(음 4/8)·추석(음 8/15)을 KASI(한국천문연구원)
  기준 음력 기준일로 내장(`_LUNAR`, 2025~2050)하고, 설날·추석 3일 연휴를 자동 확장합니다.
- **대체공휴일 규칙 자동 적용** — 2023 개정 기준: 설날·추석은 일요일/타공휴일 겹침만, 그 외(삼일절·
  어린이날·광복절·개천절·한글날·성탄절·부처님오신날)는 토·일/타공휴일 겹침에 대체일을 부여하고,
  신정·현충일은 대체 없음. 다음 평일(공휴일·주말 제외)로 자동 배정합니다.
- **2027년 설날 1일 오류 정정** — 기존 하드코딩(2027-02-05~08)이 음력 기준일을 하루 잘못 잡고 있던 것을
  KASI 기준(2027-02-06~09)으로 바로잡았습니다.
- 선거일 등 계산 불가한 임시공휴일은 `_HOLIDAY_SPECIAL` 목록으로 함께 입력(예: 2026-06-03 지방선거).
- 내장 범위(2025~2050) 밖 연도는 안내 토스트로 직접 지정을 유도합니다.
- 검증: 실제 프론트 코드(`misc-features.js`)를 그대로 로드해 2025~2050 전 연도를 KASI 골든셋과
  대조하는 테스트 추가(`tests/test_holidays.py`, `scripts/verify_holidays.mjs`).

---

### 🔐 데이터 보호 (최우선)
- **닫힌 암호화 프로필의 비밀번호 변경 시 DB가 영구 복호화 불능이 되던 치명 버그 수정**
  — 옛 salt로 복호화에 성공한 뒤에만 salt를 교체합니다.
- 열린 프로필의 비밀번호 변경 후 평문 DB를 복원해 세션 유지 (빈 DB 재암호화 전손 방지).
- 프로필 열기 실패 시 기존 프로필 상태를 보존 (비밀번호 오타 → 데이터 전손 경로 차단).
- 크래시 후 재오픈 시 더 최신인 평문을 낡은 암호화본으로 덮어쓰지 않음.
- 암호화 쓰기를 원자적으로(tmp+replace), 0바이트 스텁이 정상 암호화본을 덮지 못하게 방어.
- 서버 shutdown 훅 + Electron 종료 시 프로필 close를 기다린 후 종료 (평문 잔존 방지).
- 간호사 전원 삭제 후 재오픈 시 예시 재시드가 저장 근무표를 삭제하던 문제 수정.
- 개발자 '예시 재생성'이 빈 테이블만 남기던 트랜잭션 버그 수정.
- 스케줄 저장에 잠금·메모·공휴일·전월N·점수·로그 보존 (typed 모델 회귀 복구) +
  자동저장이 항상 무음 실패하던 payload 수정.

### 🧮 솔버 정확성
- 완화 모드에서 잠긴 셀 옆 금지 전환·N→휴무→D·시니어리티 제약이 빠지던 문제 수정 (양 엔진).
- CP-SAT 완화에 주휴 재배치·주→OF 금지·주당 주휴 ≤1 추가 (HiGHS와 의미 일치).
- 전달이월된 전월 말 연차/생휴가 생성을 무조건 실패시키던 문제 수정.
- 전월 야간전담→일반 전환 + 홀짝월 합산 규칙 조합이 무조건 실패하던 문제 수정.
- 1일이 주기 경계와 일치하는 달(예: 2026-11)에 월 경계 검증이 사라지던 문제 수정.
- 야간전담: 월중 전입/전출 시 재적일수 비례 야간 일수, 5일 윈도우 월 경계 검사.
- 야간 공정 배분에서 야간전담·야간 불가 간호사 제외 (지표 상수화 방지).
- CP-SAT 중지 버튼이 즉시 동작 (stop_search), 중지 후 완화 재시도 안 함 (양 엔진).
- 일별 요구 0명도 '정확히 일치'로 강제. 동시 생성 방지 원자화. 레이스 패자의 늦은
  등록으로 '생성 중' 고착되던 문제 수정.

### 🔍 진단·처방
- 진단 모델을 본 솔버와 동일 도메인으로 (전입/전출·사전입력 전용 코드·임산부 게이팅)
  — '원인 불명'/오진 대폭 감소.
- 10초 시간 초과를 infeasible로 오인해 멀쩡한 단계를 원인으로 지목하던 문제 수정.
- 자동 수정 처방(MCS)에 누락됐던 연속야간 휴식·홀짝월 제약 추가, 잠긴 셀 제거 처방 차단.

### 🖥 화면
- Ctrl+Shift+Z 다시실행이 전혀 동작하지 않던 버그 수정.
- 키보드 ㅂ=병가, ㅃ(Shift+ㅂ)=법정공휴일 — '병' 입력 불가 버그 수정.
- 프로필 전환 시 잠금·메모·undo 기록이 다른 프로필로 새어 들어가던 문제 수정.
- 주휴 추천 적용을 Ctrl+Z로 되돌릴 수 있게, 다중해 비교에 잠금 반영.
- 데스크톱 앱에서 저장/프로필 삭제/템플릿/프리셋의 이름 입력이 죽던 prompt() 4곳 수정.
- 희망근무 날짜 키 포맷(서버/프론트) 불일치로 전부 무시되던 문제 수정.

### 🔁 2차 정밀 재검토 (생성 엔진 4관점 리뷰 + 전건 수정)
- 제약 상호충돌 9건·모듈 무결성 8건·진단 경로 6건 추가 수정 (월경계 역사 소급,
  완화 주휴 게이트 엔진 불일치, `_pre_soft` 누수, 레이스 패자 취소 불능 등).
- 13단계 진단에 '연속야간 후 휴무'·'임산부 P1' 단계 추가 + 진단 결과 셀 점프 지원.
- 발산했던 게이팅 로직 5종을 공용 헬퍼로 통합 (사본 동기화 버그 구조적 차단).
- 완화를 2단계 사전순 솔브로 — 사전입력 보존을 증명된 최적으로 고정한 뒤 배점 최적화.

### ⭐ 위시오프 파이프라인 (신규) — 거절 분쟁의 투명화
- **위시 엑셀 붙여넣기**: '📋 엑셀 붙여넣기' 모달에 적용 대상 선택(사전입력/★희망) 추가 —
  팀즈 공유 위시 시트를 그대로 붙여넣으면 희망근무로 일괄 등록(서버 저장). 휴무 계열
  코드는 'OFF 희망'으로 정규화되어 **가능하면 반영**되는 소프트 신청이 됩니다.
- **셀 위시 입력**: 사전입력 셀 편집 팝업에 ★OFF/D/E/N 희망 버튼 — 확정(사전입력)과
  희망(소프트)을 구분해 입력.
- **위시 반영 리포트**: 생성 후 간호사별 신청/반영 수·미반영 목록(셀 점프 칩)·전체
  반영률을 표시 — "왜 내 위시가 잘렸는지"가 표로 공개됩니다.
- **위시 공정성 보정(자동)**: 직전 3개월 저장본에서 간호사별 위시 거절 횟수를 집계해
  다음 생성에서 그 간호사의 위시 가중을 자동 상향(×1.5/건, 최대 ×3) — '지난달
  거절당한 사람 우선'을 솔버가 기억합니다. 보정분은 점수 상세에 '위시 공정성 보정'
  별도 행으로 투명하게 표기.

### ⚖️ 공정성 원장·휴무의 질·사전입력 보조 (신규)
- **공정성 원장**: 직전 3개월 저장본에서 간호사별 누적 야간·주말·공휴일 근무를 자동 집계.
  간호사 요약 표에 '누적N·주말(3M)' 컬럼 추가, **night_fairness가 (누적+당월) 편차를
  최소화**하도록 누적 야간 오프셋을 자동 주입 — 지난달 야간이 많았던 사람은 이번 달
  적게 받습니다 (단월 공평 → 누적 공평).
- **휴무의 질 지표**: 간호사 요약 표에 '고립휴무'(양옆이 근무인 하루짜리)와
  '연속휴무'(최장 연속 휴무일) 컬럼 — 숫자만 같은 휴무 뒤의 보이지 않는 불공평 가시화.
- **실시간 인원 슬랙 경고**: 사전입력을 넣는 순간 일자별 부족("이대로면 생성 실패")·
  빡빡(여유 0)을 그리드 위에 표시, 클릭 시 해당 날짜로 점프.
- **🇰🇷 공휴일 자동 입력**: 버튼 한 번으로 해당 월 법정공휴일을 입력 (음력·연휴·대체공휴일
  자동계산은 위 '한국 공휴일 음력 자동계산' 참고).
- **🌙 전월N 자동**: 직전 달 저장 근무표에서 간호사별 야간 수를 자동 계산해 '전월N' 채움.
- **위시 엑셀**: 둘째 행이 요일(월~일)인 시트 형식 자동 인식, ⭐ 위시 붙여넣기 전용 버튼.

### 📈 생성 리포트·솔버 관측성 (신규)
- **생성 리포트**: 소요시간·최종 gap·시간-gap 곡선·정체 구간("이때 중지해도 무방")·
  변수/제약 수·완화 발동 여부를 생성 완료 후 카드로 표시.
- **실시간 정체 감지**: 3분 이상 개선이 없으면 진행 패널에 "지금 중지해도 품질 손해 ≤ gap%" 배지.
- **로그 힌트**: kTimeLimit·Presolve Infeasible 등 솔버 로그를 한국어 행동 제안으로 변환.
- **생성 이력**: 프로필 DB에 generation_runs 누적 — "직전 대비 3배 느림(사전입력 감소)" 비교 노트.
- **배점 분해**: 규칙별 득점 합계 표 — 어떤 배점이 지배했는지 보여 튜닝 피드백 제공.

---

## v4.3.7 — 2026-06-02

**제목표시줄을 앱 디자인과 통일.** 창 상단의 검은 운영체제 제목표시줄을, 앱 테마에 맞춘
자체 제목표시줄로 바꿨습니다.

### 🎨 창 제목표시줄
- **앱 테마와 일치** — 밝은 모드에선 흰색·짙은 글자, 다크 모드에선 어두운 색으로 자동 전환.
- **창 조작 그대로** — 최소화·최대화(복원)·닫기 버튼, 상단 막대 드래그로 창 이동, 더블클릭으로
  최대화/복원, 가장자리 리사이즈 모두 동작.
- 좌측에 앱 아이콘·이름 표시. 중복되던 운영체제 제목 텍스트 제거로 더 깔끔해졌습니다.

> 기능 동작은 v4.3.6과 동일. 화면 상단 외형만 바뀝니다.

---

## v4.3.6 — 2026-06-02

**업데이트 설치 매끄럽게.** 기존 버전이 깔린 PC에서 새 설치 파일을 실행하면 깔끔하게
업데이트되도록 설치 마법사를 보강했습니다. (기능 동작은 v4.3.5와 동일)

### 🛠 설치 / 업데이트
- **실행 중에도 업데이트 가능** — 설치 시 실행 중인 앱(Electron 창 + 내부 서버)을 자동으로 닫아,
  "파일이 사용 중" 오류나 재부팅 요구 없이 진행됩니다(Windows Restart Manager + 강제 종료).
- **이전 버전 자동 정리** — 같은 위치에 새 버전을 올리기 전에 이전 버전을 조용히 제거해,
  더 이상 쓰지 않는 파일이 남지 않습니다. (별도로 "프로그램 제거"를 먼저 할 필요 없음)
- **데이터 보존** — 프로필·근무표·DB(`%AppData%\NurseScheduler`)는 업데이트·제거 시에도 유지됩니다.

> 동일한 설치 ID를 쓰므로 새 버전은 **항상 기존 설치를 업데이트**하며, 두 버전이 함께 깔리지 않습니다.

---

## v4.3.5 — 2026-06-02

**임산부 모성보호 + 실행 시 크롬창 중복 오픈 수정.**

### 🤰 임산부 모성보호 (신규)
- 간호사 편집에서 **임산부 + 임신 구간(초기/출산 전)** 을 지정하면 솔버가 자동으로:
  - **임부휴무(P1)** — 각 구간의 완전한 주마다 1회 자동 배치 (새 근무 코드, 휴무류)
  - **야간 제외** — 임신 전 기간(초기 시작 ~ 출산 전 종료) N/NC 미배정
  - **생리휴가 면제** — 임신 전 기간 동안 생(生) 미배정 (사전입력 생도 무시)
  - **야간전담 자동 해제** — 임신 중인 달은 정규 근무로 전환
- 사전입력 그리드에서 P1을 직접 지정할 수도 있습니다. HiGHS·CP-SAT·충돌분석 동일 적용.
- 회귀 테스트 12건 추가(양 엔진), 전체 54건.

### 🐛 버그 수정
- **앱 실행 시 크롬 창 중복 오픈 제거** — Electron이 자체 창으로 UI를 띄우는데도 별도로
  브라우저가 `localhost`에 열리던 문제 수정(Electron 판별 실패). 이제 앱 창만 뜹니다.
  (`py main.py` 단독 실행 시에는 기존처럼 브라우저가 정상적으로 열립니다.)

### 📝 문서
- README 주요 기능을 기술·사용자 관점으로 정리, 상단에 파이프라인 다이어그램 추가.
- 앱 '정보' 화면의 솔버 설명을 듀얼 엔진(HiGHS·CP-SAT) 기준으로 갱신.

---

## v4.3.4 — 2026-06-02

**최소 침습 완화 + UI/UX 다듬기.** 사전입력(간호사 개인의 시간)을 "수술처럼 최소 침습"으로
다루도록 완화·진단을 강화하고, 사용성 취약점 12가지를 보완했습니다.

### 🩹 최소 침습 완화 — 휴무 보호
- **휴무 우선 보호**: 완화·자동수정이 사전입력을 바꿀 때 **근무를 먼저 조정**하고, **휴무
  (OFF·V·생·특·공·법·병)는 최후에만** 건드립니다(주휴는 고정). "휴무를 뺄 바엔 인원 추가·수요
  감축을 먼저 제시".
- **꼭 필요한 만큼만**: 유지 보너스가 배점을 사전순으로 지배 → '점수를 더 따려고' 사전입력을
  불필요하게 바꾸지 않음. 변경은 실현가능성에 필요할 때만.
- **CP-SAT 자동완화 추가**: 기존엔 진단만 했던 CP-SAT도 HiGHS와 동일한 최소 침습 완화 수행.
- **투명성·안전장치**: 완화/처방 결과를 "근무 N건 / ⚠휴무 N건(불가피)"로 분리 표기, 휴무 제거가
  포함된 처방은 적용 전 확인을 받음.

### ✨ UI/UX 개선 (12)
- 도움말 모달 확대(840px) + FAQ 갱신 + 단축키표에 마우스 제스처(더블클릭·우클릭·드래그).
- **엑셀 붙여넣기**: FAQ 안내 + 모달에 형식 예시·인식 코드 범례, 입력칸 확대.
- 사전입력 셀 hover 툴팁(더블클릭/우클릭/드래그/키 입력)으로 편집법 안내.
- 장시간 생성 시 진행바가 "멈춘 듯" 보이던 문제 → 예상 초과 시 **진행 중 애니메이션 + 안내**.
- 접근성: 모달 `role=dialog`/`aria-modal`, 상태·알림 `aria-live`, 아이콘 버튼 `aria-label`.
- 설정 탭 시작 안내 배너, 모바일 보기-전용 안내, 죽은 사이드바 마크업 정리.

> 기능 동작은 v4.3.3과 호환. 완화/처방의 '무엇을 보호하고 무엇을 바꾸는지'가 더 안전·투명해졌습니다.

---

## v4.3.3 — 2026-06-01

**내부 구조 개선 (모듈화).** 사용자 기능 변경은 없습니다 — 거대 단일 파일들을 도메인별로
분리해 유지보수성을 높였습니다. 동작은 v4.3.2와 동일.

### 🧩 코드 모듈화 (순수 리팩터링)
- **프론트 JS**: `app.js` **2744 → 529줄**. 도메인 로직을 `frontend/js/modules/` **14개 모듈**로
  분리(analysis·paste-import·profiles·dev-tools·nurse-manage·settings-defs·solver·view-helpers·
  preinput-io·grid-interactions·schedule-features·misc-features + 기존 undo-redo·drag-select).
  window-namespace 합성(`...XxxModule()`) 패턴, 빌드 도구 불필요.
- **백엔드**: `scheduler.py` **2258 → 409줄**. HiGHS 엔진을 믹스인 2개로 분리 —
  `scheduler_highs_constraints.py`(하드 제약 + 목적함수), `scheduler_highs_diagnosis.py`(13-phase
  infeasible 진단).
- **CSS**: `app.css` **1724줄 → 4파일**(tokens·base·components·yginvest-skin), cascade 순서 보존.
- 검증: pytest 37/37 + 브라우저 E2E(컴포넌트 422키·콘솔 0)로 동등성 확인. 게터는 본체 유지(스프레드
  compose 함정 회피).

---

## v4.3.2 — 2026-05-31

**테이블 전체화면 보기 + 버튼 가독성.** 근무표를 한눈에 검토할 수 있는 전체화면 모드와
선택된 프리셋 버튼의 가독성 수정.

### 🖥 테이블 전체화면 보기 (사전입력·근무표)
- 두 탭에 **⛶ 전체화면** 토글 추가 — 켜면 상단 앱바·하단 네비·탭 툴바·옵션 패널·범례를
  모두 숨기고 **슬림 배너(년월·날짜·셀 진행률) + 표만** 남겨 화면을 꽉 채운다.
- **Esc** 또는 **종료** 버튼으로 빠르게 빠져나오기. 다른 탭으로 이동하면 자동 해제.
- 근무표 탭에는 전체화면 전용 슬림 배너(년월 셀렉터 + Gap 배지 + 종료)를 표시.

### 🎨 선택 버튼 가독성 수정
- `.btn-xs`가 배경을 연한 색으로 덮어써, 선택된 프리셋 버튼(오차 **2%**, 시간 **20분** 등)이
  *연한 배경 + 흰 글자*로 거의 안 보이던 문제 수정 → 본래 강조색(짙은 배경 + 흰 글자) 복원.
  라이트/다크 모두 대비 17:1 이상.

---

## v4.3.1 — 2026-05-31

**충돌 제약 완전정복 + 두 엔진 레이스.** v4.3.0의 정밀 충돌 분석을 한 단계 더 — 어디가
충돌인지 짚는 데서 그치지 않고 **무엇을 빼거나 줄이면 풀리는지 처방**하고, 그 위치로
**바로 점프·원클릭 적용**까지. 거기에 두 솔버를 동시에 달리게 하는 레이스 모드.

### 🎯 충돌 분석 강화 (전 하드 제약 + MUS/MCS)
- **커버리지 확대**: 충돌 게이팅을 7종 → **전 하드 제약**으로 확대. 9개 금지전환·charge
  시니어리티·연속근무/야간·N→OF→D·월 야간·홀짝월 합산·연속야간 후 휴무까지 게이팅되어
  어떤 하드 제약 조합이 충돌해도 짚어낸다.
- **#1 최소 MUS**: 충돌 집합을 삭제 필터로 최소화 → 군더더기 없는 핵심 충돌만 보고.
- **#2 다중 충돌 열거**: 찾은 MUS를 가정에서 제외하며 재탐색 → **독립적인 충돌을 여러 개**
  분리 제시(각각 따로 해결 가능).
- **#3 MCS 최소 수정 처방** (`POST /api/suggest-fix`): "이 사전입력을 빼거나(비용 1) 이
  인원을 늘리면/수요를 줄이면(비용 10) 풀린다"를 MaxSAT식 최소화로 계산.
- **#4 정량 슬랙**: 남은 부족을 "○○일 D 2명 부족"처럼 정량 보고.

### 📍 충돌 위치 점프 + 자동 수정 적용 (UI)
- 정밀 충돌 분석 결과의 각 충돌이 **클릭 가능한 칩** → 사전입력 탭의 해당 셀로 이동하며
  강조(셀 단위=금지전환, 날짜 단위=일별인원/charge, 행 단위=간호사 제약).
- **"🔧 자동 수정 처방"** 버튼 → 처방 표시 + **"✅ 처방 모두 적용"**으로 사전입력 제거 +
  수요 감축을 한 번의 Undo 스냅샷으로 원자 적용(Ctrl+Z 취소 가능).

### ⚡ 두 엔진 레이스 (race)
- 생성 패널 솔버 토글에 **'⚡ 레이스'** 추가 — HiGHS와 CP-SAT를 **동시 실행**해 먼저 해를
  찾은 엔진을 채택하고 패자는 즉시 중지. 문제마다 빠른 엔진이 달라도 항상 빠른 쪽을 얻는다.
- `solver_progress`를 다중 어댑터 레지스트리로 확장(레이스 안전) — 패자만/전체 취소 구분,
  진행률은 더 앞선 엔진 표시. 단일 엔진 동작은 동일.

### 🔧 성능 · 빌드 · 테스트
- **CP-SAT 포트폴리오 병렬**: `num_workers`(최대 8, 코어 캡) + 시드 고정으로 탐색 가속.
- **build.bat**: 설치파일 echo가 `setup.iss`의 버전을 자동 추출(하드코딩 `v4.0.exe` 제거).
- 야간전담(당월 14일·N/NC only·생휴 31일달) + 생리휴가 월 1회 단위 테스트 보강. 회귀 37/37.

---

## v4.3.0 — 2026-05-31

**CP-SAT 듀얼 솔버.** 기존 PuLP+HiGHS(MILP)에 Google OR-Tools **CP-SAT**를 두 번째
생성 엔진으로 추가. 생성 패널에서 선택(기본 HiGHS). 브레인스토밍→스펙→계획→TDD 구현.

### 🧮 듀얼 엔진 아키텍처
- 공유 `_SchedulerBase`(엔진 무관 데이터/날짜/추출/점수) + 엔진별 서브클래스:
  `NurseScheduler`(HiGHS, 무손상) / `CpSatScheduler`(CP-SAT, cp_model).
- CpSatScheduler: 전 하드 제약(1일1근무·일별인원·charge·시니어리티·금지전환·연속·
  주휴/OF·V·야간전담·생휴) + 목적함수(scoring_rules 정수 가중합) 1:1 포팅.
- 진행/중지/로그를 솔버 무관 레지스트리(`solver_progress.py`)로 분리 → CP-SAT도
  `CpSolverSolutionCallback`로 동일 UX(중지·실시간 gap·로그).

### 🔍 정밀 충돌 분석 (핵심)
- `conflict_analyzer.py`: 하드 제약을 enforce 리터럴로 게이팅 +
  `SufficientAssumptionsForInfeasibility()` → infeasible 시 **어느 제약이 동시 충족
  불가인지 1회 호출로 한국어 핀포인트** ("03/12(목) D 필요 인원 5명" 등).
- `POST /api/diagnose` + 생성 결과 "🔍 정밀 충돌 분석 (CP-SAT)" 버튼 — 어느 엔진이든
  infeasible에 호출. CP-SAT 생성 실패 시 자동 첨부.

### 🎛 UI / 배포
- 생성 패널 솔버 토글(HiGHS/CP-SAT). CP-SAT 선택 시 완화 토글 비활성(완화는 HiGHS 전용).
- ortools 오프라인 PyInstaller 번들(`collect_all`+`collect_dynamic_libs`+protobuf).
  인트라넷 무인터넷 동작 검증 완료. 설치파일 ~190MB.
- 테스트 양엔진 파라미터화 + 동등성 테스트. 회귀 25/25.

---

## v4.2.1 — 2026-05-30

### 🎯 적응형 헤더 축소
Claude Design 핸드오프(YGinvest 데스크톱)의 신규 기능 이식. 활성 탭 콘텐츠를 스크롤하면 상단 앱바가 부드럽게 축소된다 — 병원 부제·버전칩·스텝 설명 숨김 + 브랜드마크/이름/스텝 패딩 축소 + 미묘한 그림자. 근무표에 더 많은 세로 공간 확보.

- 프로토타입의 `.vp-body`(660px) 내부 스크롤 패턴 대신, 우리의 영구 flex 앱바 구조에 맞춰 **캡처-단계 scroll 리스너**로 `main-content` 내부 스크롤을 감지해 `.appbar.condensed` 토글
- 탭 전환 시 `$watch('activeTab')`로 압축 해제 (새 탭 top=0)
- 모바일은 앱바가 hide되므로 영향 없음

---

## v4.2.0 — 2026-05-30

**YGinvest 핀테크 전면 리디자인.** Claude Design 핸드오프(`NurseScheduler - YGinvest`)를 실제 코드에 적용.

### 🎨 디자인 시스템 교체 (Clinical Paper → YGinvest)
- **토큰**: 따뜻한 페이퍼 베이지 + 세브란스 네이비 → **쿨그레이 바탕 `#F4F5F7` + 흰 카드 + 딥 잉크 브랜드 `#0F1115`**
- **타입**: Newsreader serif + JetBrains Mono 제거 → **Pretendard 단일** (오프라인 번들, `.serif`/`.mono` 리매핑)
- **셰이프**: 직각 0~2px + hairline → **큰 둥근 모서리(12~20px) + 부드러운 그림자**, pill 버튼·칩
- **KR 시그널**: 부족·경고 red↑(`#E84B5A`), 여유·정상 blue↓(`#2563EB`)
- **근무 팔레트**: D=블루 · E=레드 · N=바이올렛 · V=그린 (시드 `database.py` 갱신)
- 라이트/다크 모드 모두 재정의

### 📱 모바일 '오늘' 홈 보강
- 오늘 근무 현황 **shift-stack** (D/E/N 배정/요구) + 듀티별 **cov-row**(차지 배지 링·근무 시간)
- 헬퍼: `todayKey / todayShiftCount / todayShiftReq / todayNursesByDuty`
- 5-탭 하단 네비(오늘·사전입력·스케줄·분석·더보기) YG 스타일(상단 accent 바)

### 🗑 단일 디자인으로 통합
- `/legacy` (Premium UI v4.0.x) 라우트·정적 마운트·`frontend/legacy/` 트리 제거
- 디자인 토글 버튼 + `switchDesign()` + localStorage 리디렉트 제거
- YGinvest 단일 디자인으로 확정

---

## v4.1.1 — 2026-05-29

솔버 진단 로그 전반 강화 + UI 시각화.

### 🔍 진단 메시지 (백엔드)

- **Phase 4** (역순 전환) — 사전입력 **셀 기여도 ranking** 추가.
  여러 위반에 동시 참여한 셀을 빈도순으로 표시 (`이 셀을 비우면 N건 동시 해소`)
- **Phase 8** (야간전담 D/E 부족) — Phase 5와 동일한 수치형 액션 제안 패턴 적용:
  - 정규 간호사 +N명 추가 (월 슬롯 환산)
  - 야간전담 K명을 정규로 전환 (N -14×K ↔ D/E +22×K)
  - 부족 일자 D/E 인원 감축
- **Phase 9** (Charge 시니어리티) — 같은 후임 charger가 여러 날 반복되면
  "가장 빈번한 충돌 유발 간호사 N건 일괄 처리" 우선순위 제안
- 신규 헬퍼 `_rank_cells_by_violation_count()` — 위반 참여 셀 빈도 ranking

### 🎨 진단 메시지 (프론트엔드)

- `★` 표시 라인을 **앰버 배경 + 좌측 보더**로 시각 강조
- infeasible 결과 카드에 **액션 버튼** 추가:
  - 📋 진단 메시지 클립보드 복사
  - 📅 사전입력 탭으로 점프 (날짜 MM/DD 패턴 감지 시)
  - 📊 분석 탭으로 점프 (주차/주간 키워드 감지 시)

### 🧪 회귀 테스트

- `test_phase4_cell_ranking` 추가 — 셀 기여도 ranking 출력 검증
- 전체 11/11 통과 (0.53초)

---

## v4.1.0 — 2026-05-28

대규모 UI 리디자인 + 솔버 진단 강화 + 테스트 회귀망 + 코드 모듈화.

### 🎨 디자인 선택 시스템 (신규)

기존 **Premium UI**(v4.0.x — Outfit + Noto Sans KR, modern blue)와
새 **Clinical Paper UI**(v4.1 — Pretendard + Newsreader + JetBrains Mono,
세브란스 navy + paper beige)를 **둘 다** 포함.

- 기본 진입: 새 Clinical Paper UI (`/`)
- 레거시 진입: `/legacy` 경로 또는 사이드바 "디자인: 신버전" / appbar "UI ▸ Premium" 버튼
- 선택은 `localStorage.design` 에 기억 — 다음 방문에 자동 복원
- 두 디자인 모두 동일 백엔드 API 사용, 데이터/프로필 호환

### 🩺 Clinical Paper 리디자인 (Phase 6 — 완료)

- **타이포그래피**: Newsreader serif headings + JetBrains Mono 셀/수치 + Pretendard Variable 본문
- **컬러**: 세브란스 navy #023671 + 따뜻한 paper beige (#f3eee3) + 잉크 그라데이션
- **레이아웃**: 상단 마스트헤드 + 5-step stepper (사이드바 제거, 데스크톱)
- **헤더 컴팩션**: appbar/stepper/month-banner padding 축소로 테이블 영역 +50px 확보

### 🐛 헤더 sticky 투명도 수정

- `.cy-1~4` 주기 헤더, `.g-sat-bg` 토요일 헤더가 `rgba 0.06` 단일 레이어라
  스크롤 시 본문이 비쳐보이던 문제 해소
- 솔리드 `var(--card-warm)` 베이스 + 색조 오버레이로 분리
- `.g-fix`의 `background: ... !important` shorthand가 tint를 덮어쓰던 충돌 해결

### 🔍 솔버 진단 메시지 강화

[`scheduler.py` `_diagnose_infeasibility()` Phase 5] 주휴/OF 부족 출력에
구체적 액션 제안 추가 — 일반 조언 대신 수치 기반 3택:

1. **간호사 +N명 추가** (가장 부족한 주의 gap÷5 올림)
2. **요일별 D/E/N 합계 일평균 -K명** (현재 X.X → 목표 Y.Y)
3. 사전입력 휴가/OF 일부 제거

### 🧪 pytest 회귀 테스트 (10건, 0.5초)

- `tests/conftest.py`: `LimitedScheduler` 픽스처 + 빌더
- `tests/test_constraints.py`: smoke / 9개 금지 전환 / 일별 인원 정확 일치 /
  charge 시니어리티 / V 월 한도 / 1일1근무 / 사전 고정 유지
- `tests/test_diagnostics.py`: Phase 5 액션 제안 메시지 / infeasible 응답 형태
- `pytest.ini` + `tests/__init__.py` 추가

### 📦 JS 모듈화 (패턴 확립)

- `frontend/js/modules/undo-redo.js` — `_pushUndo / undo / redo` 추출
- `frontend/js/modules/drag-select.js` — 드래그 멀티셀렉트 5개 메서드 추출
- `app.js` 진입점에서 `...UndoRedoModule(), ...DragSelectModule()` 스프레드
- 빌드 도구 없이 `<script>` 순차 로드 + window namespace 패턴 — 점진적 분리 가능

### 🐞 알려진 사소한 시맨틱

- `Rules.maxVPerMonth=0`은 `_c_max_v_per_month`에서 "제약 미적용" 의미
  (UI는 별도 `unlimited_v` 플래그 사용). 회귀 테스트 주석으로 명시.

---

## v4.0.8 — 2026-04-28

간호사 일괄 관리와 사전입력 엑셀 붙여넣기 중심의 큰 UX 개선.

### ✨ 새로운 기능

#### 사전입력 엑셀 붙여넣기
Teams 공용 엑셀에서 표 영역을 그대로 복사 → 사전입력 탭의 **📋 엑셀 붙여넣기** 버튼 → 한 번의 클릭으로 적용.
- 이름 컬럼 / 날짜 헤더 행 **자동 감지** (4가지 조합 점수 비교 후 최선 선택)
- 헤더 없어도 동작 — 당월 1일부터 자동 매핑
- **한글 별칭** 자동 변환: `낮→D`, `저녁→E`, `야간→N`, `오프→OF`, `데이/이브닝/나이트차지` 등
- 다양한 날짜 형식 인식: `5/3`, `2026-05-03`, `1(일)`, `5월 3일`, 단순 숫자
- `-`, `없음`, `x` 등은 비우기 신호로 처리
- 적용 전 **미리보기 모달**: 변경/비움/미인식 코드/매칭 실패 이름 배지 + diff 표

#### 간호사 인라인 편집 + 일괄 작업
간호사 목록을 스프레드시트처럼 직접 수정.
- 셀 클릭으로 **이름/그룹/성별/가능 근무/야간전담** 즉시 변경 — 모달 안 열어도 됨
- 가능 근무: chip 토글, 성별/야간: 단일 토글 버튼
- 다중 선택(체크박스) + 일괄 작업 툴바
- 일괄 변경: **그룹 chip 클릭 / 성별 / N월 야간전담 ON·OFF / 삭제(인라인 confirm)**
- 그룹 history (localStorage) + 표준 `[A,B,C]` 합집합 — 모두 같은 그룹이어도 다른 옵션이 항상 chip으로 노출
- 그룹 input `maxlength=3` + `field-sizing: content` (적응형 폭)

#### CSV 일괄 등록 대폭 개선
- **인코딩 자동 감지** (UTF-8-sig / CP949 / EUC-KR / UTF-8) — 한국 엑셀이 그대로 저장한 파일도 받힘
- `id` 빈 칸이면 자동 생성, **이름+그룹**으로 기존 nurse 매칭하여 ID 보존 (재업로드 안전)
- `POST /api/nurses/import/preview` 신규 — DB 변경 전 추가/수정/삭제 후보 계산
- **미리보기 모달** — diff 표 + 구조화된 경고(행/컬럼/받은값/기대값/메시지) + 병합/교체 토글
- 드래그 앤 드롭: 간호사 카드 위에 CSV 파일을 끌어다 놓으면 바로 미리보기

### 🎨 UI/UX 다듬기
- **사이드바 병원 로고**: 흰 카드 배경 제거 → 투명 배경(PNG 본래 모습), 기본 숨김 — 개발자 설정에서 토글
- **개발자 설정 모달**: 폭 480→720px 확대, 다크모드 배경 fix (`--bg-card` 투명 → `--modal-bg`), 다른 모달과 톤 통일
- **사전입력 footer 행** (D/E/N 배정·필요인원): 다크모드 색상 톤다운 (반투명 컬러 틴트 + 밝은 채도 글자)
- **간호사 표 헤더**: `white-space: nowrap` (예: "5월 야간" 줄바꿈 방지)
- **모달 capable_shifts undefined 경고** 가드 (콘솔 스팸 제거)

### 🔧 인프라
- 정적 파일(`/css /js /lib /fonts /assets`)에 `Cache-Control: no-cache` 미들웨어 추가
- ETag / Last-Modified 헤더 제거 (브라우저 304 conditional 차단으로 stale stylesheet 방지)

---

## v4.0.7 — 2026-04-20

- 용인세브란스병원 로고 사이드바 추가 + 로고 크기 확대 (36→60-110px auto-scale)
- v5 Severance 대규모 리디자인은 가독성 문제로 롤백 (v5-severance 브랜치는 보존)
- assets/ 디렉터리 정적 파일 마운트 누락 fix (로고 404 해결)

## v4.0.6 — 2026-04-19

- 유령 간호사 방어 (cleanup_orphan_nurse_refs) — 삭제된 간호사 ID 캐스케이드 정리
- 사전입력 저장/복원 누락 필드 9건 fix: locked_cells, cell_notes, holidays, prev_day_reqs, prev_month_nights
- W 키로 사전입력 탭에서 주휴 빠른 입력
- CLAUDE.md 전면 갱신
- `.superpowers/` gitignore

## v4.0.5 — 2026-04-19

- 다크모드 카드 배경 버그 fix (`var(--card)` → `var(--bg-card)`) — 프로필창·온보딩 모달 흰 배경 깨짐 해결
- `build.bat`에서 `build/` 전체 삭제하던 게 icon.ico를 지워서 빌드 실패하던 문제 fix

## v4.0.4

- PyInstaller `--windowed` (console=False) 환경에서 `sys.stdout=None` 크래시 fix
- 숫자 input 폭을 em 단위로 변경 + webkit/moz 스피너 숨김

## v4.0.3

- 토스트 히스토리, Undo 카운터, 프린트 등 UX 개선

## v4.0.2

- 중간번 포함 9개 금지 전환 추가 (E→D/D1/중, N→E/D/D1/중, 중→D/D1)

## v4.0.1

- 차등 사전입력 보너스 (`preBonusLeave: 5000` / `preBonusWork: 500` / `preBonusRest: 300`)

## v4.0.0

- 초기 Electron 포팅 + 프로필 시스템 (Fernet + PBKDF2 암호화)
