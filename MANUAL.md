# NurseScheduler v4 — 사용 매뉴얼

## 1. 소개

**NurseScheduler**는 간호사 3교대 근무표를 자동으로 생성하는 데스크톱 앱입니다.

- 수리최적화 듀얼 엔진(HiGHS · OR-Tools CP-SAT)으로 최적의 근무표를 계산합니다.
- 인터넷 연결이 필요 없는 완전한 로컬 실행 앱입니다.
- 프로필별 데이터베이스 분리 + Fernet 암호화로 간호사 정보를 보호합니다.

---

## 2. 설치 방법

### 설치 버전 (권장)

1. **NurseScheduler_Setup_v4.11.0.exe** 다운로드 (Windows) — macOS는 `NurseScheduler_v4_mac_arm64.dmg`
2. 실행 → 설치 마법사 진행
3. 바탕화면 또는 시작메뉴의 **NurseScheduler** 바로가기 클릭

### 포터블 버전

1. **NurseScheduler_v4_portable.zip** 다운로드
2. 임의 폴더에 압축 해제
3. **NurseScheduler.exe** 더블클릭

### 다운로드
- GitHub Releases: https://github.com/yuangunn/nurse-v4/releases

### macOS (Apple Silicon)
1. **NurseScheduler_v4_mac_arm64.dmg** 다운로드 → 열어서 앱을 Applications로 드래그
2. 첫 실행 시 **우클릭 → 열기** (미서명 앱이라 1회 허용 필요)

### 시스템 요구사항
- Windows 10/11 (64bit) 또는 macOS (Apple Silicon, 14 Sonoma+ 권장)
- Python, Node.js 설치 불필요 (모두 번들됨)
- 인터넷 연결 불필요
- 디스크 500MB, RAM 2GB 이상

---

## 3. 퀵가이드 — 5단계로 근무표 만들기

### STEP 1: 설정 (첫 1회만)

1. **간호사 등록**: + 추가 버튼으로 개별 등록, 또는 **엑셀(xlsx) 일괄 등록**
   - **템플릿** 다운로드 → 엑셀에서 편집 → **가져오기** (4.15 참고)
   - 성별·야간전담·주휴요일은 **드롭다운**으로 고르게 돼 있어 오타가 나지 않습니다
   - 드래그로 순서 변경 = 시니어리티 (위 = 선임, 차지 우선)
2. **요일별 필요 인원**: D/E/N 인원 설정 (자동배정 근무는 행 자동 추가)
3. **규칙 관리**: 하드 제약 (연속 근무, 전환 금지 등) + 소프트 배점

### STEP 2: 사전입력

1. **주휴(주)** → **연차(V)** → **생리휴가(생)** → **희망 근무** 순으로 입력
2. **셀 클릭** → 근무 선택 팝업, 또는 **키보드 직접 입력** (D/E/N/V/O)
3. **방향키**로 셀 이동, **Del**로 삭제, **Ctrl+Z**로 되돌리기
4. **드래그**로 여러 셀 선택 후 한 번에 채우기 가능
5. **🇰🇷 공휴일 자동 입력** 버튼으로 해당 월 법정공휴일을 한 번에 입력 (4.9 참고)
6. 팀즈 위시 시트는 **📋 엑셀 붙여넣기 / ⭐ 위시 붙여넣기**로 희망근무 일괄 등록 (4.10 참고)
7. 붙여넣는 표가 **두 달에 걸쳐 있어도 그대로** 들어갑니다 — 전월 말 이월 구간이나
   두 달치 표를 잘라 낼 필요가 없습니다 (4.14 참고)
8. 표 위 **생성 가능성 신호등**(🟢/🔴)이 입력할 때마다 지금 상태로 생성이 되는지 미리
   알려 줍니다 (4.16 참고)
9. 빈 셀은 솔버가 자동으로 채웁니다
10. **바뀌면 안 되는 날은 🔒 잠금** — 사전입력 셀 팝업의 [🔒 셀 잠금] (4.5 참고). 사유는 우클릭 메모에

### STEP 3: 분석

1. **분석 실행** → 일자별 D/E/N 인원 과부족 확인
2. **주휴 추천 배분** → "사전입력에 적용" 클릭으로 한 번에 반영
3. 경고 확인: 인원 부족/빡빡한 날짜 미리 파악

### STEP 4: 스케줄 생성

1. **"생성" 버튼** 클릭 → 솔버가 최적 근무표 자동 생성
2. 오차(MIP Gap): 정밀(0%) ~ 10% 조절 가능 (높을수록 빠름)
3. **이미 완성된 표를 넣었다면 완화를 켜지 마세요** — 빈칸 없이 채운 날·주·달은
   앱이 '주어진 사실'로 받아들여 그대로 생성됩니다 (4.13 참고). 완화를 켜면
   멀쩡한 표를 솔버가 고쳐 버립니다.
4. 그 밖의 생성 실패 시: **사전입력 완화** → **주휴 무시** → **V 무제한** 순서로 체크
5. 완성된 근무표는 셀 클릭으로 수동 수정 가능 (빨간 테두리 표시)

### STEP 5: 저장 & 내보내기

1. **저장** 버튼으로 DB에 보관 (생성 완료 시 자동 저장)
2. **CSV** 버튼으로 엑셀 호환 파일 내보내기
3. **인쇄** 버튼으로 A3/A4 인쇄
4. 지나간 근무표는 저장 탭 **[📥 근무표 업로드]** 로 엑셀 파일째 올릴 수 있습니다 —
   연간 번표(시트 12개)도 한 번에 월별로 나눠 저장됩니다 (4.14 참고)

---

## 4. 주요 기능 상세

### 4.1 프로필 시스템
- 병동별 프로필 생성 (예: 101병동, 102병동)
- 프로필별 별도 DB (간호사/스케줄/규칙 분리)
- 비밀번호 설정 시 Fernet 암호화 적용
- 게스트 모드: 비밀번호 없이 즉시 사용, 종료 시 데이터 삭제

### 4.2 간호사 관리
- 그룹(A/B/C), 성별, 가능 근무 설정
- 야간전담 간호사: 월별 ON/OFF 토글
- 트레이닝 간호사: 프리셉터 연동, 트레이닝 기간 설정
- 전입/전출 로테이션: 날짜 범위 설정, 범위 밖은 자동 제외
- 엑셀(xlsx) 템플릿 다운로드 / 내보내기 / 가져오기 — 드롭다운·안내 시트 포함 (4.15 참고)
- 임산부(모성보호) 구간 설정 → 임부휴무(P1) 자동 배치 (4.17 참고)

### 4.3 근무 유형 (기본 17종)

**배정** 열이 핵심입니다. `자동` = 솔버가 알아서 놓을 수 있는 코드, `사전입력` =
**사람이 넣어야만 들어가는 코드**입니다. 이 구분이 "인원은 딱 맞는데 생성이 안 되는"
가장 흔한 원인입니다 — 5장을 함께 보세요.

| 코드 | 이름 | 시간 | 배정 |
|------|------|------|------|
| DC | Day Charge | 06:00~14:00 | 자동 |
| D | Day | 06:00~14:00 | 자동 |
| D1 | Day1 (상근·교육) | 08:30~17:30 | 사전입력 |
| EC | Evening Charge | 14:00~22:00 | 자동 |
| E | Evening | 14:00~22:00 | 자동 |
| 중 | 중간번 | 11:00~19:00 | 사전입력 |
| NC | Night Charge | 22:00~06:00 | 자동 |
| N | Night | 22:00~06:00 | 자동 |
| OF | Off | — | 자동 |
| 주 | 주휴 | — | **사전입력** |
| P1 | 임부휴무 | — | 자동 (임산부 구간에만) |
| V | 연차 | — | 자동 (월 1회) |
| 생 | 생리휴가 | — | 자동 (여성·월 1회) |
| 특 | 특별휴가 | — | 사전입력 |
| 공 | 공적업무 | — | 사전입력 |
| 법 | 법정공휴일 | — | 사전입력 (공휴일 날짜에만) |
| 병 | 병가 | — | 사전입력 |

**트레이니 표시** `/D` `/E` `/N` — 프리셉터 근무에 `/` 를 붙인 출력 전용 코드입니다.
인력 계산에는 세지 않습니다.

> ⚠️ **주휴(주)는 솔버가 놓지 못합니다.** 사람이 사전입력으로 넣어야 합니다
> (분석 탭의 '주휴 추천 배분 → 사전입력에 적용'). 주휴를 안 넣으면 남는 인력이
> 쉴 코드가 OF·V·생뿐이라 생성이 실패할 수 있습니다 — 5장 참고.

### 4.4 근무 전환 규칙
허용되는 전환 (다음날):
- D → E, N, 중, 휴무 (순방향)
- E → N, 휴무
- 중 → E, N, 중, 휴무
- N → 휴무 (연속야간 후 2일 휴무 규칙 적용 가능)

금지되는 전환:
- E → D, D1, 중 (역순)
- N → D, D1, E, 중 (역순)
- 중 → D, D1 (시간 부족)

### 4.5 사전입력 완화
생성 실패 시 "사전입력 완화"를 체크하면 사전입력한 근무를 솔버가 변경할 수 있습니다.
평소(완화 OFF)에는 사전입력은 **주어진 사실**이라 절대 바뀌지 않습니다.

완화를 켜도 **뒤집는 셀 수가 가장 적은 안**을 먼저 확정한 뒤에야 배점을 맞춥니다(최소 침습).
그리고 종류별로 뒤집히는 순서가 다릅니다 — 숫자가 클수록 늦게 뒤집힙니다:

| 종류 | 기본 보너스 | 설명 |
|------|:---------:|------|
| 휴가 (V/생/특/공/법/병) | 5000 | 간호사 요청 — 거의 유지됨 |
| 쉬는 날 (OF/P1) | 3000 | 개인의 시간 — 강하게 보호 |
| 근무 (D/E/N/중 등) | 500 | 가장 먼저 교체 |
| 주휴 (주) | 300 | "주휴 무시"를 켰을 때만 변수가 됨 |

스케줄 탭의 "완화 설정"에서 슬라이더로 조절 가능합니다. 뒤집힌 셀은 결과에서
`원래 → 배정`으로 표시됩니다.

#### 🔒 셀 잠금 — 사연 있는 날 지키기
사전입력(원티드)에는 모두 사연이 있습니다 — 제사, 생일, 가족여행, 결혼, 부모님 외래 동행.
그중 **무슨 일이 있어도 바뀌면 안 되는 날**은 사전입력 셀을 클릭해 **🔒 셀 잠금**을 누르세요.

- 잠긴 셀은 **사전입력 완화·주휴 무시·재조정에서도 절대 바뀌지 않습니다.**
- 셀 우클릭 **메모**는 사유를 적어 두는 기록용입니다. 메모만으로는 보호되지 않습니다 —
  지켜야 하면 잠그세요.
- 잠금·메모는 사전입력 저장/불러오기와 함께 보존됩니다.

### 4.6 분석
- 일자별 D/E/N(+추가 근무) 필요 인원 히트맵
- 여유 인원 색상 표시 (여유/양호/적정/빡빡/부족)
- 주휴 추천 배분 (4주 순환 + 요일별 여유도 기반)
- 간호사별 요약 (D/E/N/휴무/주말/점수)
- 생성 결과 자동 갱신

### 4.7 스케줄 비교
스케줄 탭 "🔀 비교" 버튼으로 현재 vs 저장된 스케줄의 셀 차이를 확인할 수 있습니다.

### 4.8 규칙 프리셋
"엄격" / "유연" 등 규칙 세트를 이름으로 저장하고 한 번의 클릭으로 교체할 수 있습니다.

### 4.9 🇰🇷 한국 공휴일 자동 입력
- **공휴일 자동 입력** 버튼으로 해당 월의 법정공휴일을 한 번에 채웁니다.
- 설날·추석·부처님오신날은 **음력(한국천문연구원 기준)을 자동 변환**하고, 3일 연휴와
  **대체공휴일(다음 평일)까지 규칙대로 자동 계산**합니다. **2025~2050년**을 지원합니다.
- 선거일 등 임시공휴일은 내장 목록으로 함께 입력됩니다. 그 외 임시공휴일은 날짜 헤더
  우클릭으로 직접 지정하세요.
- 공휴일은 'OF 배정 금지', '공휴일 휴가/근무 보상' 등 근무표 규칙에 그대로 반영됩니다.

### 4.10 ⭐ 위시오프 (희망근무) 파이프라인
- **위시 엑셀 붙여넣기**: 팀즈 공유 위시 시트를 '📋 엑셀 붙여넣기'(적용 대상=★희망) 또는
  '⭐ 위시 붙여넣기'로 그대로 붙여 희망근무를 일괄 등록합니다(둘째 행이 요일인 시트도 자동 인식).
- **셀 위시 입력**: 사전입력 셀 팝업의 ★OFF/D/E/N 버튼으로 확정(사전입력)과 희망(소프트)을
  구분해 입력합니다. 희망은 '가능하면 반영'되는 소프트 신청입니다.
- **위시 반영 리포트**: 생성 후 간호사별 신청/반영 수와 미반영 목록, 전체 반영률을 표로 보여줍니다.
- **위시 공정성 보정(자동)**: 직전 3개월 위시 거절 횟수를 집계해 다음 생성에서 그 간호사의
  위시 가중을 자동 상향(최대 ×3)합니다 — '지난달 거절당한 사람 우선'.

### 4.11 ⚖️ 공정성 원장 · 휴무의 질
- **공정성 원장**: 직전 3개월 저장본에서 누적 야간·주말·공휴일 근무를 집계해, 이번 달 야간을
  **누적 편차가 줄도록** 자동 배분합니다(단월 공평 → 누적 공평). 요약 표에 누적 컬럼 표시.
- **휴무의 질**: 요약 표에 '고립휴무'(양옆이 근무인 하루 휴무)·'최장 연속휴무' 컬럼을 추가해
  숫자만 같은 휴무 뒤의 불공평을 가시화합니다.
- **실시간 인원 경고**: 사전입력 중 일자별 부족/빡빡을 그리드 위에 즉시 표시(클릭 시 점프).

### 4.12 생성 리포트 · 솔버 관측성
- 생성 완료 후 소요시간·최종 gap·정체 구간·완화 발동 여부를 카드로 보여주고, 규칙별 득점
  분해로 어떤 배점이 결과를 지배했는지 확인할 수 있습니다.

### 4.13 📌 이미 완성된 근무표를 그대로 넣기

손으로 짠 표나 지난달 표를 앱에 넣을 때, **빈칸 없이 채운 부분은 앱이 '주어진 사실'로
받아들입니다.** 규칙에 맞는지 다시 따지지 않습니다.

- **일 단위**로 하루가 다 차 있으면 그 날의 인원·차지·시니어리티는 검사하지 않습니다.
- **주·달 단위**로 다 차 있으면 그 주의 OF 횟수, 연속 근무, 월 연차 한도도 마찬가지입니다.
- 확정된 칸과 빈 칸이 섞여 있으면, 규칙 상한을 **확정된 만큼 올려 잡습니다** —
  이미 들어간 것은 그대로 두고, 빈 칸에는 규칙을 그대로 적용합니다.
- 그래서 **2주차까지만 채우고 나머지를 맡기는** 방식이 그대로 됩니다.

**규칙과 다른 부분은 막지 않고 알려만 줍니다** — `📌 규칙과 다른 사전입력 N건 —
확정으로 수용됩니다` 라는 노란 안내가 뜹니다. 빨간 경고는 '알 수 없는 코드'일 때뿐입니다.

넣는 방법은 세 가지입니다.

| 방법 | 위치 | 쓰는 때 |
|---|---|---|
| **✅ 이대로 근무표로** | 사전입력 탭 | 사전입력에 붙여넣은 표를 솔버 없이 바로 근무표로 확정 |
| **📥 근무표 업로드** | 저장 탭 | 지나간 근무표를 파일째 저장 목록에 추가 (4.14) |
| 그냥 **생성** | 스케줄 탭 | 일부만 채우고 나머지를 맡길 때 |

> **📌 이 표대로 인원** 을 먼저 누르면 그 표에서 역산한 일별 D/E/N 인원이 기준으로
> 잡힙니다. `📌 이 표대로 인원` → `✅ 이대로 근무표로` 순서로 쓰면 인원 기준까지
> 그 표에 맞춰집니다.

### 4.14 📥 근무표 붙여넣기 · 파일 업로드

**두 달에 걸친 표도 그대로** — 날짜 줄을 읽어 월을 알아냅니다. `5/26`·`5월 26일`·
`2026-05-26` 은 그대로 해석하고, 일자만 있는 표(`26 27 … 31 1 2 …`)는 일자가 줄어드는
지점을 월 경계로 봅니다. 미리보기에 `날짜 해석: 2026-05-26 ~ 2026-06-30` 과 월별 건수가
표시되니 확인하고 적용하세요.

**저장 탭 [📥 근무표 업로드]** — 이미 끝난 근무표를 저장 목록에 바로 넣습니다.

- **엑셀 파일**(xlsx/xlsm/csv/tsv)을 열거나 **Ctrl+V 붙여넣기** 둘 다 됩니다.
  병합된 셀, 날짜 서식 셀, CP949 인코딩 CSV 모두 처리합니다.
- **연간 번표처럼 시트가 여러 개면** 시트↔년월 매핑 표가 떠서, 체크하고 년/월을 고친 뒤
  **한 번에 월별 저장본 여러 건**을 만듭니다. 시트 내용·시트 이름·파일 이름 순으로
  월을 자동 추정하고, '통계' 같은 비근무 시트는 자동으로 체크가 풀립니다.
- 표에서 역산한 일별 D/E/N 인원도 함께 저장돼, 나중에 불러와 재생성할 때 그 표가
  인원 기준이 됩니다.
- 솔버를 돌리지 않으므로 **점수는 비어 있습니다** (표를 그대로 보관하는 기능입니다).
- 구형 `.xls`(97-2003)는 지원하지 않습니다 — xlsx로 다시 저장하거나 붙여넣기를 쓰세요.

### 4.15 📗 간호사 명부 엑셀(xlsx) 왕복

설정 탭 간호사 섹션의 **템플릿 / 내보내기 / 가져오기** 세 버튼입니다.

- **템플릿**·**내보내기**는 같은 형식이라, 내보내서 고치고 그대로 다시 가져오면 됩니다.
- 성별·야간전담·주휴요일·주휴로테이션·트레이닝은 **드롭다운**으로 고릅니다.
  '작성 방법' 안내 시트가 함께 들어 있습니다.
- 가져오기는 **추가/수정/삭제 미리보기**를 먼저 보여주고, 확인해야 반영됩니다.
- 예전 CSV 파일도 그대로 가져와집니다 (형식은 파일 내용으로 자동 판별).

### 4.16 🚦 생성 가능성 신호등

사전입력 탭 위쪽에 지금 입력으로 근무표가 나올지 미리 표시합니다. 셀을 고칠 때마다
다시 판정합니다.

| 표시 | 뜻 | 할 일 |
|---|---|---|
| 🟢 지금 입력으로 생성 가능 | 하드 제약 충돌 없음 | 생성 누르세요 |
| 🔴 이대로는 생성 불가 | 충돌하는 셀이 있음 | 함께 표시되는 원인 셀을 고치세요 |
| ⚪ 판정 미확정 | 구성이 복잡해 5초 안에 못 정함 | 생성해 보면 확정됩니다 |

예상 생성 시간도 함께 나옵니다. 🔴 일 때는 원인이 되는 셀을 눌러 바로 이동할 수 있습니다.

### 4.17 🤰 임산부 모성보호 (P1)

간호사 편집창에서 **임산부**를 켜고 **임신초기**·**출산전** 두 구간의 날짜를 넣으면:

- 그 구간에 완전히 포함된 주마다 **임부휴무(P1)** 를 1회 자동 배치합니다 (부분 주는 ≤1회).
- **임신 전 기간 야간(N·NC)을 배정하지 않습니다.**
- 임신 중인 달은 **생리휴가를 면제**합니다 (배정하지 않음).
- 그 달의 **야간전담이 자동으로 해제**됩니다.

P1은 임산부의 지정 구간에서만 나옵니다 — 다른 사람에게는 배정되지 않습니다.

### 4.18 🌙 야간전담(나이트킵)

- 명부의 야간전담 칸을 클릭하면 **보고 있는 달**을 켜고 끕니다. 여러 달을 지정할 수 있습니다.
- 칸에는 지정된 달이 그대로 뱃지로 보입니다 — `9월 야간전담`, `9,10월 야간전담`,
  `9월 외 7개월 야간전담`, `상시 야간전담`. 전체 목록은 마우스를 올리면 나옵니다.
- 야간전담인 달에는 **N/NC만** 배정되고, 5일 안에 야간 3회 이하, 그 달 정확히 14일 근무입니다.
- **나이트킵으로 한 야간은 '수면오프 회피'(홀짝월 합산)에 세지 않습니다.** 나이트킵과
  수면오프는 무관하기 때문입니다 — 1월 나이트킵 때문에 2월 야간을 못 받는 일은 없습니다.

### 4.19 ⚠️ 오프특근 리포트

생성 결과에 `⚠ 오프특근 N건` 이 뜨면, **휴무 자리가 모자라 주 1회 OFF를 반납한 주**가
있다는 뜻입니다. 누가 몇 주차에 반납했는지 목록으로 보여줍니다.

- 경가·조가·결원으로 휴무가 몰린 주에 **불가피할 때만** 발생합니다. 앱은 이것을
  마지막 수단으로 취급해, 다른 방법이 있으면 절대 쓰지 않습니다.
- **주휴는 반납하지 않습니다** — 최소한의 휴무 보장이기 때문입니다.
- 수당·보상은 앱 밖에서 챙기셔야 합니다. 이 목록이 그 근거 자료입니다.

### 4.20 🏥 병실 배정표(어싸인)는 별도 도구

누가 어느 방을 보는지 정하는 **어싸인**은 이 앱에 없습니다. 인트라넷에서 파일 하나로
도는 별도 도구(`어싸인 자동배정.html`)로 분리돼 있습니다.

넘기는 방법: 스케줄 탭 **[📋 어싸인용 복사]** → 어싸인 도구에서 Ctrl+V.

---

## 5. 이 프로그램이 병동 규칙을 보는 방식

"왜 이렇게 나오지?" 싶을 때 보는 장입니다. 버그처럼 보이는 동작 대부분이 여기 있습니다.

### 5.1 완성된 표는 검사 대상이 아니라 '사실'입니다

빈칸 없이 채워 넣은 날·주·달은 규칙에 맞는지 다시 따지지 않고 그대로 받습니다.
지나간 근무표는 이미 그렇게 돌아간 역사이지, 앱이 승인할 대상이 아니기 때문입니다.
규칙과 다른 부분은 **막지 않고 안내만** 합니다 (4.13).

### 5.2 주휴(주)는 사람이 결정합니다 — 앱이 놓지 않습니다

주휴는 **사전입력 전용**입니다. 요일제 4주 순환은 법정 규칙이라 앱이 임의로 정할 일이
아니라고 봤습니다. 분석 탭이 **추천**은 하지만, '사전입력에 적용'을 누르는 것은 사람입니다.

> ⚠️ **"인원은 딱 맞는데 생성이 안 된다"의 1위 원인이 이것입니다.**
> 솔버가 놓을 수 있는 휴무는 `OF`(주 1회)·`V`(월 1회)·`생`(월 1회) 뿐입니다.
> 주 5일제면 1인당 주 2칸이 쉬어야 하는데 주휴를 안 넣으면 채울 코드가 없습니다.
> **인원이 남을수록 오히려 더 심해집니다** — 남는 사람을 쉬게 할 코드가 없으니까요.
> 분석 탭 → 주휴 추천 배분 → 사전입력에 적용, 그다음 생성하세요.

### 5.3 오프특근 — 휴무가 모자랄 때의 마지막 수단

주 1회 OFF는 원칙이지만, **경가·조가 등으로 휴무가 갑자기 몰리면** 남은 사람들이
오프를 줄여 가며 근무를 뜁니다. 그것이 오프특근입니다.

- 앱은 이것을 **압도적 페널티가 붙은 마지막 수단**으로 다룹니다. 다른 방법이 있으면
  절대 쓰지 않고, 근무표가 성립하지 않을 때만 최소한으로 반납합니다.
- 발생하면 `⚠ 오프특근 N건` 으로 **누가 어느 주에 반납했는지 보고**합니다 (4.19).
- **공휴일과는 무관합니다.** 공휴일이 몰린 주에 잘 생길 뿐, 조건은 휴무 공급 부족입니다.
- 반납해도 **주휴는 지킵니다** — 최소한의 휴무 보장이기 때문입니다.

### 5.4 공휴일

- 공휴일에는 **법휴(법)·주휴(주)·근무를 모두** 넣을 수 있습니다. `OF` 만 금지입니다.
- 공휴일에도 병원은 돌아갑니다 — **전원 휴무는 불가능**합니다.
- **주휴를 공휴일에 몰아주는 관행은 없습니다.** 주휴와 공휴가 겹치면 중복수당으로
  보상하는 것이지, 날짜를 옮겨 붙이는 게 아닙니다.
- 월 경계에 걸친 주의 공휴일(예: 1월 주기의 마지막 주에 든 2월 설날)도 인식합니다.

### 5.5 연차(V)가 많이 나가는 이유

일별 인원은 **정확히 일치**입니다 — 필요 인원보다 더 출근시키지 않습니다. 그러면 남는
인력은 어딘가 쉬어야 하는데, 솔버가 쓸 수 있는 카드가 `OF`(주 1회)·`생`(월 1회)·`V`뿐이라
**남는 만큼 V로 흘러갑니다.** V에는 큰 페널티가 걸려 있어 마지막에 쓰이지만, 구조적으로
모자라면 어쩔 수 없습니다.

줄이려면 **주휴를 여유 있는 요일로 옮기는 것**이 가장 효과적입니다. 인원을 줄이거나
요일별 필요 인원을 조정하는 것도 방법입니다.

### 5.6 요일별 최소 인원

18명 기준입니다 — 일 3/4/3 · 월 4/5/3 · 화~목 5/5/3 · 금 5/4/3 · **토 4/3/2**.
공휴일이 평일에 이어지면 조절할 수 있지만 크게 벗어나지 않으며, **결원이 있어도
이 최소는 보장**합니다.

### 5.7 나이트킵 야간은 수면오프와 무관합니다

'홀짝월 합산 야간 11회' 는 **수면오프를 피하려는 규칙**이라 일반 근무로 쌓은 야간에만
적용됩니다. 나이트킵 달의 14회를 여기 합산하면 그 사람만 다음 달 야간을 못 받게 되므로,
앱은 나이트킵 달의 야간을 **제외**합니다 (4.18).

### 5.8 사전입력(원티드)에는 사유가 있습니다

제사·가족여행·결혼 준비·부모님 외래 동행 — 사전입력은 **한 사람의 한 달을 결정하는
일**입니다. 앱은 사전입력 휴가에 가장 큰 유지 보너스(5000)를 걸어 거의 손대지 않습니다.
조정이 불가피할 때도 연차 대량 사용보다 **원티드 미반영을 당사자에게 알리고 주휴를
조정하는 쪽**이 먼저입니다.

---

## 6. 키보드 단축키

| 단축키 | 기능 |
|--------|------|
| ← ↑ ↓ → | 사전입력 셀 이동 |
| D / E / N / V | 사전입력 직접 입력 |
| O | OF 입력 |
| Del | 사전입력 셀 삭제 |
| Ctrl+Z | 되돌리기 |
| Ctrl+Shift+Z | 다시 실행 |
| ? | 단축키 도움말 토글 |
| F5 | 새로고침 |
| F11 | 전체화면 |

---

## 7. 자주 묻는 질문 (FAQ)

**Q: 근무표 생성이 실패합니다 (Infeasible)**
0. **먼저 주휴를 넣으셨는지 보세요** — 가장 흔한 원인입니다 (5.2).
   분석 탭 → 주휴 추천 배분 → 사전입력에 적용.
1. 사전입력 탭의 **🚦 신호등**이 🔴이면 함께 표시되는 원인 셀부터 고치세요 (4.16).
2. 그래도 안 되면 사전입력 완화 체크 → 재시도
3. 실패 시 주휴 무시 추가 → 실패 시 V 무제한 추가
4. 에러 메시지의 원인 확인 → 해당 규칙 완화

**Q: 이미 완성된 근무표를 넣었는데 생성이 안 됩니다**
빈칸 없이 채웠다면 그대로 통과해야 정상입니다 (5.1). 확인할 것:
- **사전입력 완화를 껐는지** — 켜져 있으면 완화가 우선이라 표를 고쳐 버립니다.
- 빈칸이 남아 있지 않은지 — 한 칸이라도 비면 그 날은 규칙 검사를 받습니다.
- 솔버를 아예 안 돌리려면 사전입력 탭 **[✅ 이대로 근무표로]** 또는
  저장 탭 **[📥 근무표 업로드]** 를 쓰세요 (4.13).

**Q: 연차(V)가 너무 많이 자동으로 나갑니다**
남는 인력이 쉴 코드가 V뿐이라 생기는 구조적 결과입니다 (5.5).
주휴를 여유 있는 요일로 옮기는 것이 가장 효과가 큽니다.

**Q: 근무표에 ⚠ 오프특근이 떴습니다**
그 주에 휴무 자리가 모자라 OF를 반납했다는 뜻입니다 (4.19, 5.3).
누가 어느 주인지 목록으로 나오니 수당·보상 근거로 쓰세요.

**Q: 생성 시간이 너무 오래 걸립니다**
오차를 2%~5%로 올리면 훨씬 빠르게 생성됩니다.

**Q: 차지(Charge) 순서는 어떻게 결정되나요?**
간호사 목록 순서(위=선임, 아래=후임)가 시니어리티입니다. 드래그로 순서를 변경하세요.

**Q: 다른 PC로 데이터를 옮기려면?**
개발자 설정 → DB 백업 다운로드, 또는 간호사 **내보내기(xlsx)** 사용 (4.15).

**Q: 병실 배정표(어싸인)는 어디 있나요?**
별도 도구로 분리했습니다 (4.20). 스케줄 탭 **[📋 어싸인용 복사]** 로 근무표를
복사해 어싸인 도구에 붙여넣으세요.

**Q: 데이터는 어디에 저장되나요?**
`%APPDATA%\NurseScheduler\` 폴더에 저장됩니다. 앱을 삭제해도 데이터는 보존됩니다.

---

## 8. 개발자 모드

사이드바 `v4` 텍스트를 **5회 빠르게 클릭**하면 개발자 모드가 활성화됩니다.

개발자 설정 (사이드바 하단 "개발자" 버튼):
- 마스터 비밀번호 설정/제거
- 프로필 비밀번호 강제 초기화
- localStorage 전체 삭제
- DB 백업 다운로드
- 예시 데이터 초기화
- DB 경로/크기 확인
- 글꼴 범위 확장 (8~30px)

---

## 9. 업데이트 방법

1. https://github.com/yuangunn/nurse-v4/releases 에서 최신 버전 다운로드
2. 기존 버전 제거 (제어판 → 프로그램 → NurseScheduler 제거)
3. 새 설치파일 실행
4. 사용자 데이터는 자동 보존됩니다

---

**NurseScheduler v4.11.0** | 개발: yuangunn | https://github.com/yuangunn/nurse-v4
