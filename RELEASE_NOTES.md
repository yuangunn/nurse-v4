## 화면을 다시 입혔습니다 — 50대 컴맹도 한 번에 (클로드 디자인 핸드오프 적용)

계산 엔진과 데이터는 그대로이고 **화면만** 바뀌었습니다. 기존 기능은 하나도 빠지지 않았고(핸들러 손실 0), 디자인이 준 검사기 200여 항목을 전부 통과했습니다.

### 🎨 무엇이 달라졌나
- **글자가 커지고 버튼이 커졌습니다** — 글자 최소 14px·본문 18px, 버튼은 40px 이상. 회색 글자는 쓰지 않습니다. 화면마다 **검정 버튼은 하나** — 지금 눌러야 할 것입니다.
- **모든 버튼에 설명** — 마우스를 올리면 무엇을 하는지 한 문장으로 알려 줍니다.
- **단계 바** — 설정 ✓ · ① 사전입력 · ② 분석 · ③ 근무표. 지금 할 단계만 검정이고, 부제에 "134칸 입력됨", "부족한 날 있음", "생성 완료 · 저장됨"처럼 상태가 보입니다.
- **사전입력** — 표 위 한 문장("지금 입력으로 만들 수 있어요 · 예상 약 3분 · 134 / 558칸 입력")으로 지금 상태를 알 수 있고, 셀을 두 번 누르면 **근무 8 · 휴무 9 코드가 이름과 함께** 큼직한 팝업으로 그 칸 옆에 뜹니다. 팝업이 열린 채로도 D/E/N/V/O 키로 넣을 수 있습니다.
- **근무표** — **근무표 만들기** 하나만 누르면 진행 막대와 남은 시간이 보이고, 끝나면 "만들어졌고 저장됐습니다 (시각) · 사전입력 모두 지켰어요"가 한 줄로 나옵니다.
  만들 수 없으면 빨간 배너에 **원인과 고칠 수 있는 것 셋**(그 칸으로 가기 · 필요 인원 −1)과 **사전입력 완화 켜고 다시 만들기** 버튼이 나옵니다.
- **요약·리포트 서랍** — 요약 · 원티드 미반영(되돌려서 잠금 · 칸으로 가기) · 연차(V) 설명 · 오프특근 · 주의 · 위시 반영 · 생성 기록.
- **인쇄 미리보기** — A4 가로 · 흑백으로 어떻게 나오는지 먼저 보고 인쇄합니다.
- **설정** — 왼쪽 메뉴로 "처음 한 번"(명부 · 요일별 인원 · 규칙)과 "거의 안 바꾸는 것"(근무 정의 · 우선순위 · 그 외)을 나눴습니다. 숫자는 − / + 버튼, 명부는 줄에서 바로 고칩니다.
- **폰(보기 전용)** — 오늘(내 근무 카드 · 오늘 근무자) · 근무표(내 근무만 · 그룹 칩) · 사전입력 · 더보기(글자 크기 · 어두운 화면).
- **글꼴 선택** — 설정 → 그 외에서 Pretendard · Noto Sans KR · 나눔고딕 · IBM Plex Sans KR · 맑은 고딕 중 고를 수 있습니다(인터넷 불필요).
- 위시 시트·자동 채움·붙여넣기를 넣은 뒤에는 **되돌리기** 토스트가 뜹니다.

### 🔧 내부
- 화면 모듈 합성 방식을 고치면서 생성 리포트의 "규칙별 점수" 표가 늘 비어 있던 오래된 버그도 같이 잡혔습니다.
- 앱을 열 때 콘솔에 찍히던 경고 600여 건이 0이 됐습니다.
