## 기존 번표 반입 + 완성표 생성 보장

이미 만들어진 번표를 앱으로 들여오는 경로와, 그 표를 거부하지 않는 생성 엔진 개선이 핵심입니다.

### ✅ 완성·부분 확정 번표는 생성을 거부하지 않는다 (사실-클램프)
- 완성된 번표를 사전입력에 빈칸 없이 넣고 생성 → **그대로 생성 성공**. 인원이 빡빡하다는 이유로 거부하던 문제 해소, 규칙과 다른 부분은 참고 안내로만 표시.
- 2주차까지만 채우는 **부분 확정도 동일** — 하루가 전부 차 있으면 그 날은 확정, 빈칸에만 규칙 적용. 확정 OF 2회·V 2회 등 기존 확정은 수용하되 자유 칸이 위반을 더 늘리는 것만 금지.
- 생리휴가 강제 배정 제거(보장 아님 — 월 1회 상한만), 진단 오진 문구 교체, 분석 탭 주별 OF 2회/0회 구조 경고(자동 변환 없음 — 주휴는 사람이 결정).

### 📥 저장 탭 — 완성 번표 업로드
- 엑셀 파일(xlsx/xlsm/csv/tsv) 업로드 또는 붙여넣기로 저장 목록에 바로 추가(솔버 안 돌림, 표 역산 일별 인원 포함).
- 연간 번표는 **멀티시트 일괄 업로드** — 시트 이름·내용·파일명에서 년월을 자동 감지해 매핑 확인 후 월별 저장본 일괄 생성.

### 📆 사전입력 붙여넣기 멀티월 · 📇 간호사 명부 xlsx 왕복
- 붙여넣은 표를 1달로 끊지 않고 날짜 해석대로 해당되는 모든 달에 적용.
- 명부 템플릿/내보내기/가져오기가 드롭다운·안내 시트를 갖춘 xlsx 왕복으로 (CSV 호환 유지).

### 🚦 사전입력 실시간 생성 가능성 신호등
- 입력을 멈추면 하드 제약만 백그라운드로 1회 풀이 → 🟢 생성 가능/🔴 불가/⚪ 미확정 + 원인 칩(클릭 시 셀 이동) + 예상 생성 시간 상시 표시. 입력 즉시 잡아주는 라인트도 대폭 확장.

### 📄 assign.html v3 — 문서형 전면 재설계 (다중 PC·NAS 공유)
- 켜면 바로 '이번 주 배정표', NAS의 데이터 파일 하나로 여러 PC 공유, 전 기간 연속 타임라인(이월 개념 소멸), 배정표에서 병상·근무·담당 실시간 수정, 실제 화면 GIF 도움말·처음 안내, 이번 주 엑셀 내보내기 중심 동작.

빌드 산출물(Windows 설치파일·포터블 ZIP, macOS dmg/zip)은 CI가 자동 업로드합니다.
전체 변경 내역: [CHANGELOG.md](https://github.com/yuangunn/nurse-v4/blob/main/CHANGELOG.md)
