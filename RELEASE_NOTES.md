## 병동 제1원칙 + 오프특근 규칙

병동에서 실제로 쓰는 운영 사실을 프로젝트 제1원칙으로 못박고, 그중 하나인 **오프특근**을 생성 엔진 규칙으로 넣었습니다. 공휴일 연휴가 낀 주에서 구조적으로 풀리지 않던 교착이 해소됩니다.

### 🔁 오프특근 — 공휴일이 낀 주는 OF 0회 허용
- 종전에는 완전한 주마다 **OF 정확히 1회**. 공휴일에는 OF를 넣을 수 없으니 3~4일 연휴가 낀 주는 남은 평일 자리가 주휴 + 전원 OF로 정확히 만석이 되어 **생성 자체가 실패**했습니다(추석·설연휴 주).
- 이제 **당월 공휴일이 포함된 완전한 주는 OF `≤1`** — 불가피할 때 주 1회 OFF를 빼는 병동 관행 그대로입니다. 공휴일 OF 금지·주휴 주 1회는 변함없습니다.
- 양 엔진(HiGHS·CP-SAT)·정밀 분석·성공 결과 참고 안내·화면 경고 2곳까지 같은 기준으로 맞췄습니다 — 어느 화면도 공휴일 주의 OF 0회를 위반이라고 말하지 않습니다.

### 🚦 사전입력 신호등 거짓 빨강 수정
- 신호등이 생성 엔진과 다른(사실-클램프 없는) 기준으로 판정해, **엔진은 그대로 만들어 주는 입력에 🔴 생성 불가**를 띄우던 문제를 고쳤습니다(한 주에 신청 OFF 2개, 확정된 E→D 쌍 등).
- 정밀 분석·자동 수정 제안은 종전대로 "규칙과 다른 곳을 전부 짚어 주는" 기준을 유지합니다.

### 📋 제1원칙 · 토요일 기본 인원 4/3/2
- 공휴일에도 병원은 돌아간다 · 오프특근 · 요일별 **최소** 인원(18명 기준, **토 4/3/2**) · 주휴를 공휴일에 몰아주는 관행은 없다(중복수당으로 보상) · 대학병원 3교대 외과병동 · 사전입력(원티드)에는 반드시 사유가 있다 — 7건을 `CLAUDE.md` 최상단에 명시.
- **토요일 기본값이 3/3/2 → 4/3/2로** 바뀝니다(새 프로필 기준). 기존 프로필은 저장된 값을 그대로 쓰니, 설정 → 요일별 인원에서 토요일을 한 번 확인해 주세요.

### 🧪 12개월 연속 생성 시뮬레이터
- 전월 생성분을 이어붙여 여러 달을 연속 생성해 보는 도구(`scripts/sim_chain.py`)를 추가했습니다. 확인된 사실: **주휴 4주 순환 드리프트(주말 → 평일 몰림)는 생성을 막지 않고**, 비용이 연차로 전가됩니다.

빌드 산출물(Windows 설치파일·포터블 ZIP, macOS dmg/zip)은 CI가 자동 업로드합니다.
전체 변경 내역: [CHANGELOG.md](https://github.com/yuangunn/nurse-v4/blob/main/CHANGELOG.md)
