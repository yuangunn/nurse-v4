# NurseScheduler v4 — 마일스톤 (기능 로드맵)

> 세션이 바뀌어도 이어서 작업할 수 있도록 하는 로드맵.
> 항목마다 **배경 → 현재 상태 → 설계 → 남은 작업 → 파일 포인터** 순.
> 착수 전 [`decisions.md`](decisions.md)의 결정·네거티브 지식 확인, 완료하면 여기 상태 갱신 + 세션노트 기록.
> 마지막 갱신: 2026-08-21 (v4.10.2 기준 — v4.10.x 반영 누락분 정정)
> 최초 작성: 2026-08-18 (요청 출처: 2026-08-18 사용자 요청 3건)

상태 표기: ⬜ 대기 · 🔶 진행 중 · ✅ 코드 완료(릴리즈 대기) · 🚀 릴리즈됨

### 출하 현황 (2026-08-21)
| 태그 | 담은 것 |
|---|---|
| v4.7.0 | M1 저장탭 업로드 · M2 멀티월 붙여넣기 · M3 1차(명부 xlsx) · M4 사실-클램프 |
| v4.8.0 | 오프특근 1차(오답) · 신호등 거짓 빨강 수정 · 토 4/3/2 시드 |
| v4.9.0 | 오프특근 2차(오답) · 월경계 공휴일 · 사전입력 경고를 '참고'로 |
| v4.10.0 | **오프특근 최종안** (휴무 공급 부족 시 마지막 수단 + `off_teukgeun` 리포트) |
| v4.10.1 | 나이트킵 야간을 홀짝월 합산에서 제외 |
| v4.10.2 | 명부 야간전담 월 뱃지 |

> 회귀 **pytest 137건 + JS 5종** 통과 (2026-08-21 실측).
> 테스트는 `pip install -r requirements-dev.txt` 가 필요하다 — `requirements.txt` 만으로는
> `httpx` 가 없어 2개 파일 11건이 **수집 자체가 안 된다**(조용히 안 도는 상태였다).

---

## M1. 기존 근무표를 저장 탭에 업로드 (엑셀 파일 or 붙여넣기) — ✅ 코드 완료 (2026-08-18)

### 배경 (사용자 요청)
이미 만들어져 있는(과거·수기) 번표를 앱에 넣으려면 사전입력 탭에 붙여넣고
"이대로 근무표로"를 거쳐 다시 저장해야 했다. **저장 탭에서 바로** 엑셀 파일 업로드
또는 붙여넣기로 저장 목록에 추가되게 해달라는 요청.

### 구현된 것
- **저장 탭 → [📥 근무표 업로드]** 버튼 → 기존 붙여넣기 모달을 `target='saved'`로 재사용.
  - 붙여넣기(Ctrl+V) 또는 **[📂 엑셀/CSV 파일 열기]** 둘 다 가능. 파일은 서버
    `POST /api/parse-table-file`(openpyxl)로 파싱해 같은 매칭 파이프라인에 공급.
    xlsx/xlsm/csv/tsv 지원, **병합 셀 값 전파**, 날짜 셀은 ISO 문자열로 변환,
    CSV는 CP949 폴백. 시트가 여러 개면 데이터 많은 시트 자동 선택 + 시트 선택 UI.
  - **저장 년월 자동 감지**: 날짜 헤더에 월이 있으면 다수 월을 앵커로 자동 설정
    (`_maybeAutoAnchor`), 수동 수정 가능. 저장 이름 입력란 포함.
  - 적용 시 `POST /api/schedules`로 바로 저장 — 인식된 셀 전체(`matched_all`)를
    schedule로, **표에서 역산한 일별 D/E/N 인원을 `prev_day_reqs`로 함께 저장**
    (불러온 뒤 재조정/재생성 시 이 표가 인원 기준이 되게). 솔버는 돌리지 않고
    점수는 비어 있음 (usePrevAsSchedule과 같은 철학).
  - 명부에 없는 이름은 기존 "＋ 간호사로 추가하고 다시 맞추기" 버튼 재사용.
- 구형 `.xls`(97-2003)는 **비지원** — 안내 토스트 (xlsx 재저장 또는 붙여넣기 유도).
- **멀티시트(연간 번표) 일괄 업로드** (2026-08-18 추가 요청): 시트가 2개 이상이면
  각 시트를 년월에 매핑하는 표가 떠서 체크·년/월 수정 후 **한 번에 월별 저장본 N건** 생성.
  감지 우선순위 = 시트 내용의 명시 월(다수) > 시트 이름(`1월`·`2026년 1월`·`2026-1`) >
  파일 이름 연도(`26년 번표.xlsx`→2026) > 보고 있는 년월. "통계" 같은 비근무 시트는
  월 미상으로 자동 체크 해제. 행 클릭 = 그 시트 미리보기, 단일 시트 파일은 기존 흐름 그대로
  (시트/파일 이름 년월은 앵커로만 반영). 핵심 함수: `_analyzeBatch`/`_applyBatchSaved`/
  `_parseSheetNameYM`/`_parseFileNameYear`, 매칭 코어는 `_matchGrid`로 순수화해 공유.

### 남은 작업
- [ ] 실제 병동 xlsx 양식으로 확인 (표 위 제목 행·비고 열 등 잡음이 매칭 점수를 얼마나 흔드는지)
- [ ] 모달 위 파일 드래그&드롭 (지금은 파일 선택 버튼만; CSV 가져오기의 `onCsvDrop` 패턴 참고)
- [x] ~~MANUAL.md에 사용법 추가~~ — **완료 (2026-08-24)**: 4.13 완성표 그대로 넣기 ·
      4.14 붙여넣기/업로드 · 4.15 명부 xlsx · 4.16 신호등 · 4.17 임산부 · 4.18 야간전담 ·
      4.19 오프특근 · 4.20 어싸인 분리, 5장 '병동 규칙을 보는 방식'(제1원칙) 신설.
      버전 문자열은 v4.10.2 로 이미 동기화돼 있음.

### 파일
| 파일 | 내용 |
|---|---|
| `server/api.py` | `POST /api/parse-table-file` + `_parse_xlsx_sheets`/`_parse_delimited_sheets`/`_cell_to_str` |
| `frontend/js/modules/paste-import.js` | `target='saved'` 분기, `_applyPasteSaved`, `_loadPasteFile`/`_selectPasteSheet`, `_maybeAutoAnchor` |
| `frontend/index.html` | 저장 탭 업로드 버튼, 모달의 파일 열기·년월/이름 입력·저장 미리보기 |
| `requirements.txt` / `NurseScheduler.spec` | `openpyxl` 의존성 + hiddenimports (`openpyxl`, `et_xmlfile`) |
| `tests/test_parse_table_file.py` | 엔드포인트 회귀 6건 (xlsx 날짜/병합, CP949, 구분자, 거부 케이스) |

---

## M2. 사전입력 붙여넣기 멀티월 적용 (1달 단위로 끊지 않기) — ✅ 코드 완료 (2026-08-18)

### 배경 (사용자 요청)
사전근무 엑셀 붙여넣기가 **보고 있는 달**에만 매칭되고 그 외 날짜는 버려졌다
(날짜 셀에서 일(日)만 뽑고 월은 무시). 복사해 온 표를 분석해서 **해당되는 일자에
모두** 적용되게 해달라는 요청 — 전월 말 이월 구간이나 두 달치 표가 대표 사례.

### 구현된 것 (날짜 해석 규칙 — `_parseDateCell` + `_resolveHeaderDates`)
1. **월 명시 셀은 그대로 해석**: `5/26`, `5월 26일`, `5.26`, `2026-05-26`,
   `2026년 5월 26일`. 연도가 없으면 앵커 년월에 가장 가까운 해(동률이면 과거).
2. **일자만 있는 셀은 문맥으로 월을 이음**: 앞선 월 명시 셀에서 순방향 전파
   (일자가 줄어들면 +1개월), 첫 명시 셀 앞은 역방향 전파 (일자가 늘면 -1개월).
3. **전부 일자만이면 run 분리**: 일자가 감소하는 지점을 월 경계로 보고,
   **1로 시작하는 첫 run = 앵커 년월**(없으면 첫 run), 앞 run은 한 달씩 이전,
   뒤 run은 한 달씩 다음. `26~31 | 1~30` (이월형), `1~30 | 1~31` (두 달 연속) 모두 처리.
   단일 run `1~31`은 기존과 동일하게 당월.
4. 실존하지 않는 날짜(2/30 등)는 무효 처리. 앵커 = 보고 있는 년월
   (저장탭 업로드에서는 모달의 년월 입력).
- 적용은 dayKey(`YYYY-MM-DD`) 전체 키라 당월 밖도 `prevSchedule`에 그대로 저장 —
  달을 바꾸면 보이고, 사전입력 서버 저장/자동복원도 전체가 함께 간다.
  위시(★)도 동일 (스케줄러가 주기 밖 위시 키는 원래 무시하므로 안전).
- **미리보기 강화**: `날짜 해석: 2026-05-26 ~ 2026-06-30` 범위 표시,
  월별 적용 건수 pill(`📅 2026년 5월 6건`), 당월 주기 밖 건수 경고 문구,
  적용 토스트에도 월별 건수.

### 남은 작업
- [ ] 없음 (실사용 피드백 대기). 헤더 없는 붙여넣기(fallback)와 커서 붙여넣기는
      기존대로 당월 한정 — 여기까지 멀티월로 넓힐지는 실제 필요가 생기면.

### 파일
| 파일 | 내용 |
|---|---|
| `frontend/js/modules/paste-import.js` | `_parseDateCell`(월·연도 인식), `_resolveHeaderDates`, `_matchPasteGrid`(월별 집계·주기 밖 카운트) |
| `frontend/index.html` | 날짜 해석 범위·월별 pill·주기 밖 경고 표시 |
| `scripts/test_paste_dates.mjs` | 날짜 해석 순수 로직 검증 (node, 30여 케이스) — `node scripts/test_paste_dates.mjs` |

---

## M3. 간호사 명부 CSV 입출력 대체 — 🔶 1차(xlsx 왕복) 완료 (2026-08-18) · 2차(명부 붙여넣기)는 백로그

### 배경 (사용자 요청)
"간호사 템플릿 csv로 저장하고 내보내는 게 너무 불편해. 다른 방식으로 해줄 수
있는지 알아봐줘." — 아래 조사에 따라 **권장 1차(A. xlsx 왕복)를 구현 완료**.

### 구현된 것 (1차 — xlsx 왕복)
- `GET /api/nurses/template.xlsx` / `GET /api/nurses/export.xlsx` — 같은 형식 공유
  (헤더 서식·열 폭·틀 고정 + **드롭다운**: 성별/야간전담/주휴요일/주휴로테이션/트레이닝
  + '작성 방법' 안내 시트). 내보내서 고치고 그대로 가져오는 왕복 편집.
- 가져오기: 기존 preview/diff 모달 그대로, 파일이 xlsx면 **매직 바이트(PK)로 자동 감지**
  (확장자·파일명 무관) → `_parse_xlsx_sheets` → '이름' 헤더 있는 시트 선택
  (`_pick_nurse_sheet_rows`) → 공통 행 파서. 날짜 셀은 ISO로, '여/남'도 기존처럼 인식.
- 리팩토링: `_parse_nurses_csv(text)` → `_parse_nurses_rows(rows)` (CSV·xlsx 공용,
  `#` 주석 행 무시 유지), `_decode_csv_input` → `_decode_nurse_table_rows`,
  내보내기 행 변환은 `_nurse_to_row`로 공용화. **CSV 경로는 호환 유지**
  (`/api/nurses/template`·`/api/nurses/export` 존치, 가져오기는 CSV도 그대로 받음).
- 프론트: 템플릿/내보내기 버튼이 xlsx를 받고, 가져오기 accept·드롭존이 xlsx 허용.
- 검증: `tests/test_nurse_xlsx.py` 5건 (템플릿 구조/드롭다운·내보내기 형식·xlsx 가져오기
  왕복(날짜 셀→ISO·여→female·시트 선택)·CSV 회귀·주석 행) + E2E(설정 탭 xlsx 가져오기 → 등록).

### 현행 흐름과 불편 지점
- 템플릿: `GET /api/nurses/template` → `#` 주석 안내 행이 붙은 CSV (14컬럼:
  id·이름·그룹·성별·가능근무·야간전담·시니어리티·주휴요일·주휴자동·트레이니·
  교육종료일·프리셉터ID·전입일·전출일).
- 가져오기: `POST /api/nurses/import/preview` → 추가/수정/삭제 diff 미리보기 → `import`.
  인코딩 자동 감지(utf-8-sig/cp949) 등은 이미 견고.
- **남는 불편은 CSV라는 형식 자체**:
  1. 엑셀에서 편집 후 "CSV로 저장" 단계 필요 — 형식 손실 경고 대화상자, 시트 1개 제한.
  2. `가능근무`가 쉼표 구분(`"DC,D,EC,E"`)이라 CSV 안에서 따옴표 감싸기 필요 — 손으로 만들면 잘 깨짐.
  3. `#` 주석 행이 엑셀 화면에서 어색하고 실수로 남기면 헷갈림.
  4. wishes·night_months·임신 구간은 CSV에 아예 없음 (부분 필드만 왕복).

### 대안 비교
| 안 | 내용 | 장점 | 비용 |
|---|---|---|---|
| **A. xlsx 왕복 (권장 1차)** | 템플릿/내보내기/가져오기를 `.xlsx`로 | "CSV로 저장" 단계·인코딩 문제 소멸. 성별/Y·N/요일에 **데이터 유효성 드롭다운**, 가능근무는 근무별 O/X 컬럼으로 분해 가능. M1에서 openpyxl·`/api/parse-table-file`이 이미 들어와 비용 급감 | 서버 엔드포인트 2개(template.xlsx/export.xlsx) + 가져오기를 rows→기존 `_parse_nurses_csv` 매칭 로직에 연결 |
| **B. 명부 붙여넣기 (권장 2차)** | 파일 없이 엑셀 범위 복사→붙여넣기로 명부 등록/일괄 수정 | 파일 자체가 사라짐 — 근무표 붙여넣기와 UX 통일. 이미 붙여넣기에서 간호사 자동 추가(f6c00e0)가 있어 자연 확장 | 헤더 컬럼 자동 매핑(이름/그룹/성별…) + 미리보기 모달. paste-import 인프라 재사용 |
| C. CSV 유지 + 소폭 개선 | 주석 행 제거, 안내를 화면으로 이동 | 최소 변경 | 1·2·4 불편이 그대로 |

### 남은 작업 (2차 — 백로그)
- [ ] **B. 명부 붙여넣기**: 설정 탭 간호사 섹션에 "표 붙여넣기" 버튼 → 컬럼 매핑
      미리보기 → 적용 (paste-import 인프라 재사용). 실사용에서 파일 왕복이 불편하다는
      피드백이 나오면 착수.
- [x] ~~MANUAL.md 갱신~~ — **완료 (2026-08-24)**: 4.15 절 신설, STEP 1·4.2 의 CSV 설명 정정.

### 파일 포인터 (착수 시 볼 곳)
`server/api.py` (`_NURSE_CSV_HEADER`/`_parse_nurses_csv`/`nurse_import_preview` ~440-840행 부근,
`_parse_xlsx_sheets` ~840행 부근) · `frontend/js/modules/schedule-features.js`
(CSV 입출력 UI, `csvImportPreview`) · `frontend/index.html` (CSV 가져오기 모달)

---

## M4. "사전입력 인원 딱 맞는데 생성 실패" 근본 원인 — ✅ 조사 + 핵심 해결 구현 (2026-08-19)

### 해결 (2026-08-19 구현 — "완성된 표를 넣으면 생성이 되어야 한다")
사용자 최종 지시: "이미 완성된(지나간, 제약충돌 없는) 번표를 사전입력에 빈칸 없이
넣고 생성을 누르면 생성이 되어야 한다." 구현:
1. **완전 확정 표 폴백** (`_fully_pinned`/`_confirm_pinned_result`, scheduler_base):
   당월 재적 셀이 전부 사전입력이면, 솔버 infeasible/타임아웃 시 **표를 그대로
   근무표로 확정**(점수 계산 포함, `pinned_confirmed`) + 앱 규칙과 다른 부분은
   `pinned_notes` 참고 목록으로만 안내 (일별 인원 차이·금지 전환·주별 OF 횟수·
   V/야간 한도·연속 한도·야간전담 일수). HiGHS·CP-SAT 4개 실패 분기 모두 연결.
   완화(allow_pre_relax)를 명시로 켰을 때만 완화가 우선.
   → 완성 번표는 **검증 대상이 아니라 주어진 사실** (decisions.md 결정 기록).
2. **생휴 강제 제거**: 야간전담 여성+31일달 '생 정확히 1회' 강제를 양 엔진 +
   conflict_analyzer에서 제거 — "생휴는 보장이 아니다" 사용자 원칙. 월 ≤1 상한만 유지.
3. **진단 오진 문구 수정** (P1 부분): Phase 11의 "야간전담 1회 의무…" 오도 문구를
   '쉴 수 있는 코드 공급 부족' 설명 + 주휴 배분 안내로 교체.
4. **분석 탭 구조 경고** (P4 기각 대안 — 사용자 제안): 주별 OF 2회/0회를 분석 탭
   경고로 표시. "하나를 '주'로 바꾸면 규칙에 맞습니다" 안내만 하고 **자동 변환은
   하지 않음** (주휴는 사람이 결정). `analysis.js:_prevStructureWarnings`.
검증: 전체 pytest 115건 통과 (라운드트립·야간전담 15일 확정표·특성화 갱신 포함),
E2E에 분석 탭 경고 단계 추가 통과.

### 확장 (2026-08-19 후속) — 사실-클램프: 부분 확정 일반화 ✅
사용자 후속 지시: "완전 확정뿐 아니라 **2주차까지만 꽉 채우고** 바꾸고 싶은 부분만
비워서 생성하는 경우에도 같게 작동해야 한다. **일 단위로 모두 차 있으면 그 일에만
확정**하는 게 맞다." → 폴백(사후 구제)을 **제약 생성 시점의 일반 규칙**으로 승격:
- 확정만으로 결정되는 제약은 **스킵** (일 전체 확정 = 그 날 인원/charge/시니어리티
  + 변수 리터럴화, 쌍/윈도우/주 전체 확정 = 전환·NOD·연속·주 OF/P1, 확정 rest_d =
  restAfterNight), 확정+자유 혼합 카운트는 **상한 클램프** `max(규칙, 확정분)`
  (일별 인원·주 OF/P1·월 V/야간/생/홀짝월·야간전담 윈도우/월간). 자유 셀에는 모든
  규칙이 종전대로 걸린다 — 위반을 '더 늘리는' 건 여전히 금지.
- 완전 확정 표는 이제 폴백 없이 솔버가 **직접 성공** + `pinned_notes` 안내
  (`_attach_pin_notes`, restAfterNight·NOD 안내 추가). 폴백(1-14)은 안전망으로 유지.
- `allow_pre_relax` 명시 시 클램프 OFF (완화 우선). conflict_analyzer는 의도적
  미적용 (전수 설명 도구). 상세: decisions.md **결정 1-15**.
검증: 전체 pytest 122건 통과 — 신규 `tests/test_partial_pin_facts.py` 4건
(2주 확정+자유 주·일 단위 확정·부분 확정 날 인원 클램프·CP-SAT 패리티) 포함,
특성화·진단 테스트 기대치 갱신.

### (기록) 최초 조사 — 2026-08-18

### 배경 (사용자 요청)
"사전입력으로 인원수에 딱 맞게 입력했는데도 불구하고 생성이 되지 않는 근본적 이유를 찾아줘."
→ 재현 실험 8종으로 규명. 특성화 테스트 `tests/test_exact_fit_characterization.py`(7건)가
현재 동작을 고정하고 있으며, 개선 시 이 테스트가 기준선이다.

### 근본 원인 (실험으로 확인)
**일별 인원수는 필요조건일 뿐이다.** 진짜 병목은 간호사별 **주 단위 구조 제약**과
**휴무 코드의 공급 상한**:

1. **잉여 인력/빈 칸이 쉴 코드가 없다 (구조적 — 가장 흔한 시나리오)**
   솔버가 자동 배정할 수 있는 휴무는 `OF`(완전한 주마다 **정확히 1회** — 0회도 2회도
   하드 위반), `V`(월 1회), `생`(월 1회·여성)뿐. `주(주휴)·특·공·병·D1·중`은
   **사전입력 전용**(auto_assign=0)이라 솔버가 못 놓는다.
   → 주당 필요 휴무가 1인당 2칸 이상인데(= 주 5일제) 주휴를 사전입력하지 않으면
   채울 코드가 없어 **infeasible**. 인원이 남아돌수록 오히려 더 심해진다.
   재현: 6명·일 3명 수요·주휴 없음 → infeasible (주간 휴무 수요 21칸 > 공급 최대
   16칸 = OF 6 + V 6 + 생 4). 주휴 사전입력만 넣으면 같은 조건이 성공.
2. **사전입력은 도메인 고정이라 1칸의 위반이 전체 실패가 된다**
   허용 유연성은 D↔DC·E↔EC·N↔NC 승격(`_PRE_FLEX`)뿐. 일별 인원이 `==`(초과 불가)라
   다른 칸으로 흡수도 안 된다. 손으로 짠 표에서 흔한 것들:
   - 주말 몰아쉬기(한 주 OF 2회 / 다른 주 0회) → weekly OF `==1` 위반
   - 역방향 전환(E→D, N→E 등 9종) → 하드 금지
   - 연차 월 2회 → `maxVPerMonth=1` 하드 (unlimited_v 옵션 없이는)
3. **진단 품질 격차 (사용자가 "왜?"를 알 수 없는 이유)**
   전환·V 초과·OF 몰림(2회)은 셀 좌표까지 정확히 짚는다. 그러나 **1번(구조적 휴무
   부족)은 13단계 누적 진단의 마지막 상한 단계에서 터져 "생리휴가(생) 제약 충돌 —
   야간전담 설정을 확인하세요"로 오진**된다(V 무제한→생 무제한 순으로 흡수되다가
   생 월1회 상한이 11단계에서 마지막으로 걸리는 구조). OF 0회 주도 미표기(2회만 표기).
4. (참고) 탈출구는 이미 있다: `allow_pre_relax`(사전입력 완화)를 켜면 최소 변형으로
   성공하고 바뀐 칸이 `relaxed_cells`로 표시된다. 이미 완성된 표는 "✅ 이대로
   근무표로"·저장탭 "📥 근무표 업로드"가 솔버 없이 받는 정공법.

### 후속 조사 (2026-08-19) — 이월(전월 완성 번표) 시나리오
사용자 명확화: "완성된 번표를 빈칸 없이 채웠고, 전월 이월로 만들 때 **전월 구간에서**
오류가 난다." 풀 스케줄러 재현 9종 (`tests/test_carryover_characterization.py` 4건 고정):

- **이월 자체는 버그가 아니다** — 깨끗한 전월 기록 + 규칙에 맞는 당월 표는 정상 생성.
  전월 '내부' 완결 위반(E→D가 3/29→3/30 등)도 소급하지 않는다 (설계 일관 확인:
  전환·NOD·연속근무/야간·restAfterNight·주휴OF·V/야간/생 월한도 전부 당월/경계만).
- 오류는 **전월말↔당월초 '경계 걸침' 검사**에서 난다. 확인된 4패턴 (모두 진단이
  셀 좌표까지 정확):
  ① 경계 금지 전환 — 전월 3/31 N + 당월 4/1 D (둘 다 확정)
  ② 경계 걸침 연속근무/야간 한도 — 3/29~4/3 D 6연속 (max 5)
  ③ 전월말 연속야간 후 당월 초 휴무 부족 — N,N(3/30-31) → OF(4/1) → D(4/2)
  ④ 당월 첫 부분-주(예: 4/1~4/4)에 OF 2개 — **병동 주(월~일 등) 관행 vs 앱 주(일~토
    주기)** 불일치, 그리고 **주휴 구분 없이 OF만 쓰는 병동식 표** (완전한 주에서도
    OF 2개면 실패 — 사실상 손표 이월의 최다 원인 후보)
- 즉 "제약이 충돌하지 않으면 생성돼야 한다"는 기대는 코드상 성립한다. 문제는
  '충돌'의 기준이 병동 관행과 3곳에서 다르다는 것: 주 경계(일~토 주기), OF/주 구분,
  경계 소급 4종. 사용자는 전월 표를 '확정된 역사'로 보지만 앱은 경계에 한해
  당월 배치의 전제 조건으로 검사한다.

### 개선 제안 (다음 세션 착수 가이드 — 정책 변경은 사용자 결정 필요)
- [x] ~~**P1** 진단 개선: 주간 휴무 산술 검사~~ — **완료 (2026-08-24)**.
      `_SchedulerBase.rest_supply_shortfall()` (양 엔진 + conflict_analyzer 공용).
      주별 `휴무 수요 = 재적칸 − 근무 필요` vs `공급 상한 = 확정 휴무 + OF 1 + V + 생 + P1`.
      **수요 하한·공급 상한**이라 부족이 나오면 확정 (거짓 양성 없음).
      · 진단: 13단계를 돌린 뒤, 꼬리 단계 오진(생리휴가·홀짝월·월 최대 야간)이면
        **대체**하고 구체 원인(금지 전환 등)이 따로 있으면 **함께** 붙인다
        (`_diagnose_infeasibility` 래퍼 + `_diagnose_phases`).
      · 신호등: `probe()` 가 솔버 전에 산술로 확정 판정 + `rest_shortfall` 동봉.
        프론트는 기존 `feas.message` 렌더링을 그대로 쓴다.
      · **월 경계에 걸친 주는 판정하지 않는다** — V·생 월 한도가 다음 달 몫으로
        넘어가 공급이 실제로 더 크다(거짓 부족). 회귀로 고정.
      · 병동이 주휴·특·공을 auto_assign 으로 열어 뒀으면 판정하지 않는다
        (그 코드로 얼마든지 채울 수 있어 산술이 성립하지 않음).
      · 회귀 `tests/test_rest_supply.py` 9건. 특성화 기준선 갱신(오진 문구 → 산술 확정).
      · 부산물: `PROD_SHIFTS` 픽스처의 `법` 이 auto_assign 기본값(True)이라
        실제 DB(`database.py:150` pre_input_only)와 달랐다 → 정정.
- [x] ~~**P2** OF 0회 주 표기~~ — **기각** (v4.10.0). OF 0회는 규칙 위반이 아니라
      **오프특근의 결과**다(제1원칙 3). 사전입력 경고·분석 탭·`pinned_notes` 세 곳에서
      의도적으로 제거했으니 되살리지 말 것 (`analysis.js:130`, `grid-interactions.js:183`,
      `scheduler_base.py:970` 에 주석으로 박아 둠). 2회(상한 초과)는 그대로 경고한다.
- [ ] **P3 (정책 — 사용자 확인 후)**: ⓐ 주휴 자동배정 옵션(juhu_day 규칙대로 솔버가
      '주' 배치 — 지금은 분석 탭 추천→사전입력 적용이 수동 필수 단계),
      ⓒ 완성표 감지 시 "이대로 근무표로/업로드" 경로 안내 배너 (버튼은 상시 노출돼 있고
      감지 배너는 없음 — 다만 사실-클램프 이후 완성표는 솔버가 그냥 성공하므로 시급도 낮아졌다).
      ~~ⓑ weekly OF `==1`의 유연 모드~~ → **완료 (v4.10.0)**: `of_sum + slack == bound`
      + 목적함수 −1,000,000×slack. 상한은 하드, 1회 의무는 페널티 — 성립 불가일 때만 반납하고
      `off_teukgeun` 으로 보고 (`_c_weekly_off`, 결정 1-16).
- [x] ~~P4 자동 '주' 변환~~ — **기각** (2026-08-19 "주는 사람이 결정할거야").
      대안으로 **분석 탭 구조 경고** 구현 완료 (위 '해결' 4번).
- [ ] **P5 (정책 — 사용자 결정, 보류)**: '전월 기록 존중 모드' 토글 — 완전 확정 표는
      이제 폴백이 흡수하므로, 남는 케이스는 '전월 확정 + 당월 일부만 입력'의 경계
      걸침뿐. 실사용 불편이 다시 보고되면 검토.
- [x] **P6 완료** (2026-08-19): 야간전담 여성+31일달 '생 정확히 1회' 강제 제거
      (위 '해결' 2번 — 생휴는 보장 아님).
- 관련 파일: `scheduler_highs_diagnosis.py`(13단계), `scheduler_highs_constraints.py:_c_weekly_off`,
  `api.py:/api/feasibility`, `tests/test_exact_fit_characterization.py`(기준선),
  `tests/test_feasibility_probe.py`.

---

## M5. 탐구: 오픈웨이트 LLM(파인튜닝) 기반 근무표 생성 — 📋 탐구 완료 · 구현은 보류 권장 (2026-08-19)

### 질문 (사용자)
"오픈웨이트 AI 모델을 이 프로그램에 학습시킨 것(규칙·배점·과거 표)으로 파인튜닝하면
근무표 생성을 자연스럽게 하지 않을까? 지금은 infeasible일 땐 그냥 반환해버리잖아.
이 방법으로 어떻게 가능할지 탐구해봐."

### 결론 요약 (정직하게)
1. **LLM 단독 생성은 불가** — 하드 제약(일별 인원 `==`·전환 금지·주휴/OF·시니어리티)을
   *보장*할 수 있는 언어모델은 없다. 솔버가 항상 최종 검증자여야 한다.
2. **"infeasible이면 그냥 반환"이라는 전제는 이번 세션에서 이미 크게 바뀌었다** —
   사실-클램프(M4 확장)·완화(allow_pre_relax)·13단계 진단·정밀 충돌 분석
   (`suggest_correction`)이 그 자리를 채웠다. LLM의 남는 자리는 "그 출력을 사람 말로
   풀어주고 수정안을 제안하는 UX 레이어"다.
3. **파인튜닝은 데이터·하드웨어·유지보수 3중 장벽** — 병동 하나의 실데이터는 월 1건
   (연 12건)뿐이고, 인트라넷 PC(GPU 없음)에서 쓸 만한 크기의 모델은 못 돌린다.
   지금 단계에서는 **비권장**. 하면 아래 로드맵의 ①②(파인튜닝 불필요)부터.

### 1. LLM 단독 생성이 안 되는 이유
- 18명×31일 = 558셀 × 17코드. 한 칸만 틀려도(인원 1명 초과, E→D 1건) 표 전체가
  무효인 문제 — 언어모델의 "거의 맞음"은 이 도메인에서 0점과 같다.
- 간호사 근무표(NRP)는 NP-hard. 제약 만족을 *생성 확률*로 보장하는 방법은 현재
  연구 수준(제약 디코딩·neuro-symbolic)에서도 이 정도 제약 밀도에선 미해결.
- 문헌의 현실적 합의는 "LLM은 모델러/수리 제안자, 솔버가 검증자" 구조
  (OR 분야의 LLM 활용 연구들 — 예: 자연어→모델 변환, 휴리스틱 제안). 우리 앱은
  이미 모델이 코드로 완성돼 있으므로 '모델러' 역할도 필요 없다.

### 2. 우리 데이터의 실제 가치 (파인튜닝 말고)
- **과거 확정 표** (schedules/prev_schedules): 파인튜닝 샘플로는 절대량 부족
  (수천~수만 건 필요 vs 연 12건). 대신 ⓐ 배점 규칙 가중치의 **역추정**(inverse
  optimization — 과거 표를 가장 잘 재현하는 scoring_rules 가중치 탐색, LLM 불필요)
  ⓑ 프롬프트 컨텍스트(예시 표 2~3건 + 규칙 JSON)로 충분히 활용 가능.
- **솔버 합성 데이터 자기증류** (솔버 최적해 + 무작위 섭동 → '깨진 표→수리' 쌍
  대량 생성)는 기술적으로 가능하지만, **솔버가 이미 답을 아는 문제를 LLM에게
  가르치는 순환**이다. 가치가 있는 건 '자연어 설명' 능력뿐인데, 그건 파인튜닝
  없이 범용 모델 + 좋은 프롬프트로 이미 된다.

### 3. 현실적 접점 — LLM+솔버 하이브리드 4가지 (가치 순)
| # | 아이디어 | 구조 | 평가 |
|---|---|---|---|
| H1 | **진단 통역/수리 제안 레이어** | 13단계 진단 + `pinned_notes` + `suggest_correction` 출력(JSON)을 LLM에 → "3/4 김OO의 E를 OF로 바꾸면 풀립니다" 같은 자연어 요약 + 수정안 → **수정안은 반드시 솔버 재검증** | 가장 가치 높음. 파인튜닝 불필요 — 프롬프트로 충분 |
| H2 | **웜스타트 힌트** | LLM 초안 → CP-SAT `AddHint()`로 주입 → 솔버가 수리 | 이미 사전입력+완화가 같은 역할. 속도 이득 불확실 — 실익 낮음 |
| H3 | **선호 역추정** | 과거 표 → scoring_rules 가중치 추천 | LLM 없이 역최적화가 정공법. LLM은 라벨링 보조 정도 |
| H4 | **자연어 조작 인터페이스** | "김지현 다음주 야간 빼줘" → wishes/사전입력 변경 명령 파싱 | 데모성 가치. 그리드 직접 편집이 이미 빠름 |

### 4. 오프라인 인트라넷 배포 제약 (하드웨어가 결정적)
- 병동 PC는 GPU 없는 사무용 — llama.cpp(GGUF Q4) 기준 7B급도 CPU에서 수 tok/s.
  H1의 짧은 설명문(수백 토큰)은 견딜 만하나, 표 생성(수천 토큰)은 분 단위 — UX 불가.
- 한국어 가능 오픈웨이트 후보(참고): Qwen 계열, EXAONE(LG), Llama, Gemma 등.
  파인튜닝 자체는 LoRA/QLoRA로 소비자 GPU 1장에서 가능하지만, **추론을 배포할 곳이
  없다**는 게 병목. Electron 번들에 GGUF 동봉 시 설치본 +4~8GB.
- 개발/온라인 환경이라면 파인튜닝 대신 Claude API로 H1을 즉시 프로토타입 가능
  (Messages API + 진단 JSON 컨텍스트 — 앱의 오프라인 원칙과는 별개 트랙).

### 5. 권장 로드맵 (착수한다면 이 순서)
1. **H1-정적**: LLM 없이 — `suggest_correction`(MUS/MCS) 결과를 진단 메시지에
   자연어 템플릿으로 합치기. 지금 코드로 절반 이상 도달 가능.
2. **H1-LLM(개발자 도구)**: 개발 환경 한정, 진단 JSON → Claude API → 수리 제안 →
   솔버 재검증 루프 프로토타입. 효과가 입증되면 그때 오프라인화를 고민.
3. **H3-역최적화**: 과거 표 6건 이상 쌓이면 scoring_rules 가중치 자동 추천.
4. **오픈웨이트 파인튜닝**: 위가 다 되고, 병동 PC에 GPU/NPU가 생기고, 실데이터가
   수백 건 규모가 됐을 때 재평가. 그 전엔 비용 대비 가치 없음.

> 요컨대: "infeasible이면 그냥 반환"은 이제 사실이 아니게 됐고(사실-클램프·완화·
> 진단·충돌 분석), LLM이 이기는 지점은 **설명과 제안의 자연어화**뿐이다. 그건
> 파인튜닝이 아니라 좋은 프롬프트 + 솔버 재검증 루프의 일이다.

---

## 백로그 (세션 중 발견, 우선순위 낮음)
- ~~**공휴일 몰린 주의 구조적 교착 + 오프특근 규칙**~~ — **완료 (v4.10.0 최종안)**.
  트리거를 두 번 잘못 잡았다: v4.8.0 '공휴일 낀 주는 누구나', v4.9.0 '그 주 공휴일에
  법휴·근무가 있는 사람만' — **둘 다 제1원칙 3의 *예시*를 *조건*으로 착각**한 것.
  실제 트리거는 **휴무 공급 부족**이고 공휴일과 무관하다. 최종안은 슬랙+압도적 페널티
  (위 M4-P3ⓑ). 결정 1-16. **공휴일을 조건으로 되돌리지 말 것.**
- **연차(V) 사용을 줄이는 다음 레버 — 주휴 조정** (2026-08-20 시뮬 v3):
  배점 규칙을 넣으면 V는 5개월 60 → 14건으로 줄고(생휴가 먼저 소진), 남는 V는
  ①생 월 1회 상한을 다 쓴 여성 ②남성의 구조적 잔여다. 제1원칙 8("V 자동 대량
  사용은 납득 불가 — 원티드 미반영 안내·주휴 조정이 우선")을 더 만족시키려면
  분석 탭 주휴 추천이 **슬랙이 남는 요일로 주휴를 옮기는 제안**을 해야 한다.
  (요일제 4주 순환은 법정 규칙이라 사람이 결정 — 앱은 제안만.)
- **CP-SAT 실패 시 정밀 분석 라벨의 클램프 불일치** (2026-08-19): infeasible 원인
  라벨이 무클램프 게이티드 모델에서 나와, 사실-클램프가 수용하는 항목(확정 OF 2회
  주 등)이 원인으로 표시될 수 있음 — 진짜 원인이 가려짐. 게이티드 모델 클램프
  적용(라벨용 별도 인스턴스) 검토. 신호등 프로브 1단계는 수정 완료 (결정 1-15).
- **원티드(사전입력) 사유 기록** (2026-08-20): 제1원칙 8은 "사전입력에는 반드시
  사유가 있다"인데 앱에는 사유 필드가 없다(셀 메모 `cell_notes`로 대신 가능).
  완화(`relaxed_cells`)로 뒤집힌 셀에 "왜 반려됐는지"를 사유와 함께 보여주는
  화면이 있으면 제1원칙 8의 '원티드 미반영 안내'가 완성된다.
- 붙여넣기 모달 파일 DnD (M1 잔여와 동일)
- Alpine 템플릿 null-접근 콘솔 오류 27건 — 프로필 열 때부터 발생하는 기존 노이즈
  (`diagResult.*`, `wishReport.*`, `nurseModal.data.pregnancy.*` 등을 x-text가 가드 없이 참조).
  동작엔 지장 없으나 디버깅 시 시야를 가림. `?.` 가드 일괄 정리 후보.
- E2E 스모크 자동화 — 이번 세션에서 playwright로 게스트 진입→붙여넣기→저장까지
  검증하는 스크립트를 썼음 (레포 밖 스크래치). 반복될 것 같으면 `scripts/e2e_smoke.mjs`로 승격.
