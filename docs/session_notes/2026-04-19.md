# 세션 노트 — 2026-04-19

## 세션 요약
이전 세션에서 v4.0.0~v4.0.4 릴리즈 완료. 이번 세션 시작 시 컴팩팅 수행.
본 세션에서는 **다크모드 렌더링 버그 수정** + **문서 보존 작업** 진행.

---

## 완료된 작업

### 1. 다크모드 카드 배경 버그 수정
- **증상**: 다크모드에서 프로필 선택 화면(`profileScreen`)과 온보딩 모달(`showOnboarding`) 카드가 하얗게 렌더링 + 텍스트 밝은 회색 → 안 보임.
- **근본 원인**: HTML/CSS에서 `var(--card, #fff)` 사용했는데 실제 정의된 변수는 `--bg-card`. fallback `#fff`가 항상 적용돼서 다크모드에서도 흰 배경.
- **수정 내역**:
  - `frontend/index.html`: lines 15, 76, 106, 126, 165, 175, 176, 185, 244, 1405 — `var(--card,...)` → `var(--bg-card,...)`
  - `frontend/css/app.css`: lines 564 (`.profile-card`), 589 (`.toast-history-btn`) — `var(--card)` → `var(--bg-card)`
- **검증**: `grep "var(--card" frontend/` → 0건.
- **미검증**: Electron에서 실제 렌더 확인은 안 됨 (빌드 필요).

### 2. 문서 보존
- `docs/decisions.md` 작성 — 아키텍처 결정(12개) + 네거티브 지식(13개) + 사용자 규칙 + 버전 이정표.
- `docs/session_notes/2026-04-19.md` (이 파일) 작성.

---

## 남은 작업

### 릴리즈 v4.0.5 필요
- 다크모드 수정분 반영 빌드 + 릴리즈.
- `electron/package.json`, `installer/setup.iss`, `frontend/index.html`의 버전 문자열(1624 라인 `v4.0.4`) 동시 업데이트 필요.
- 빌드 명령: `build.bat` (원클릭, PyInstaller → @electron/packager → ZIP → ISCC).

### 미확정 이슈
- (없음 — 현재 로그상 다크모드 이슈 외 보고된 버그 없음)

---

## 핵심 파일 위치 맵 (재시작 시 참고)

| 파일 | 설명 |
|---|---|
| `main.py` | 진입점. `_ensure_stdio()`, `main()`, stdout `PORT:` 출력 |
| `server/api.py` (~616줄) | FastAPI 라우터 — 프로필, 간호사 CSV, 스케줄, 개발 모드 API |
| `server/scheduler.py` (~2029줄) | MIP 솔버. 주요 함수: `_c_forbidden_transitions()` @ 773, `_c_daily_requirements()` @ 640, `_solve_with_relaxed_pre()` @ 402, `SOLVER_SHIFTS` @ 107 |
| `server/profiles.py` | Fernet 암호화, `force_reset_password()`, `open/close/create/delete_profile()`, 게스트 모드 |
| `server/models.py` | Pydantic — `preBonusLeave/Work/Rest`, `restAfterNight*`, `start_date/end_date`, `is_trainee` 등 |
| `frontend/index.html` (~1700줄) | 5탭 SPA + 프로필 오버레이 + 온보딩 7슬라이드 + 도움말 5탭. 버전 표시 라인 1624 |
| `frontend/js/app.js` (~2000줄) | Alpine app 팩토리. `reqShiftCodes`, `reqFooterRows`, `_checkViolations()`, `onGridKeyDown()`, `trackVersionClick()`, 토스트 시스템 |
| `frontend/css/app.css` | CSS 변수(`--bg-card`, `--border`, `--accent` 등), `.sidebar-logo 18px!important`, `.name-cell sticky!important`, 다크모드 `html.dark {...}` |
| `electron/main.js` | Python 자식 프로세스 스폰, `PORT:` 파싱, 헬스 체크 폴링, 싱글 인스턴스 락 |
| `electron/preload.js` | `electronInfo` 컨텍스트 브릿지 (현재 `isElectron`, `version`만) |
| `electron/package.json` | Electron 메타 + electron-builder 설정 (실사용은 @electron/packager) |
| `installer/setup.iss` | Inno Setup 6 스크립트. `AppVersion` 동기화 필요 |
| `NurseScheduler.spec` | PyInstaller. `collect_all('highspy', 'cryptography', 'pulp')`, `console=False` |
| `build.bat` | 원클릭 빌드. ISCC 경로: `%LOCALAPPDATA%\Programs\Inno Setup 6\ISCC.exe` |
| `MANUAL.md` (209줄) | 사용자 매뉴얼 (8섹션) |
| `README.md` (134줄) | 리포 소개 |
| `CLAUDE.md` | 프로젝트 전역 맥락 (불변 영역) |
| `docs/decisions.md` | 아키텍처 결정 + 네거티브 지식 (세션 간 공유) |
| `docs/session_notes/YYYY-MM-DD.md` | 세션별 일지 |

---

## 커밋 이력 (최근)
```
c375148 fix: 모든 숫자 인풋 폭을 em 단위로 변경 + 스피너 숨김
cdb104b chore: v4.0.4
b1e047d fix: PyInstaller --windowed에서 sys.stdout=None 크래시 수정
4219740 docs: 라이선스 문구를 All Rights Reserved로 명확화
e20caa3 docs: README.md 추가
```

## Git 상태 (세션 시작 시)
- branch: main, origin/main과 동기화 완료
- untracked: `.claude/` (작업 디렉토리)
- 이번 세션 변경: `frontend/index.html`, `frontend/css/app.css`, `docs/` (신규)

---

## 재시작 시 추천 명령

```bash
# 1) 상태 확인
git status
git log --oneline -5

# 2) 다크모드 수정 검증 (수동 테스트)
cd c:\Users\Helios_Neo_18\nurse-v4\.claude\worktrees\exciting-tesla
py main.py
# 브라우저에서 사이드바 하단 테마 토글 클릭 → 프로필 화면·온보딩 확인

# 3) 커밋 (승인받은 후)
git add frontend/index.html frontend/css/app.css docs/
git commit -m "fix: 다크모드에서 프로필창·온보딩 모달 카드 배경 복구 (--card → --bg-card)"
```
